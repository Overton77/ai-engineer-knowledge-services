# WS-08 structured extraction publication recovery review

2026-09-06 UTC

EV-086 accepts internal fenced publication recovery for structured extraction. Configured activity/worker and authenticated public reads remain next; this is not complete extraction-service admission.

`PostgresStructuredExtractionRecoveryStore.readPublished({lease,signal?})` queries committed success/failure publication identity under the original operation and an exact live step/holder/token/fence. It returns undefined if no publication is committed, and fails closed for contradictory or unavailable committed metadata. It authorizes a fresh resolver specifically for verification_replay, checks the complete registered handle, byte limit, native hash/size and canonical JSON, authenticates the server-owned Ed25519 seal and native transformation binding through the accepted/failure result helpers, then compares the derived terminal result against the exact SQL-owned result. A final operation/lease check follows artifact I/O and signature verification. Returned manifest/artifact/result values are frozen and discriminated as accepted or failed.

Recovery reads the original immutable publication bytes. It has no provider-dispatch port and never reconstructs providerCall from current accounting rows. Thus the committed cost-state snapshot is preserved. This turn does not claim a live post-publication supplier reconciliation or process-kill test; those remain separate evidence requirements. The store is internal to an active leased worker and intentionally is not a terminal public result-read API.

Actual local accepted recovery passes35 checks and failure recovery passes29. Both Gateway and Interfaze recover exact manifest/artifact/terminal result under higher-fence leases and reject stale/wrong-holder claims, unknown signing keys, pre-cancelled requests and corrupted actual Storage bytes. The accepted proof ends with two succeeded operations/receipts plus its pre-existing late-binding negative; the failure proof ends with four failed operations/receipts. All calls are injected synthetic transport. The accepted proof's three calls settle10 synthetic micros each; the four failure calls retain400 total micros liability with unknown actual costs. These totals describe these final proofs only, not all historical fixture activity. An initial accepted recovery run stopped because its old fixture allowed admission purpose only; the fixture was updated to explicitly permit verification_replay for its configured tenant. Production authorization was not loosened.

Independent audits download27 accepted-path and32 failure-path Storage objects, verify complete handles, native hashes/signatures/parents and original result equality, and check terminal receipt/event/outbox/lease/operation chains. They compare20 and15 scoped source files and13 and8 installed SQL function bodies respectively. Tenant isolation and denial of direct app_reader receipt access remain verified. The original EV-083/085 audit files were preserved; recovery audits have new filenames. Standalone proof typechecks and all72 uncached workspace typecheck/test/build tasks pass (2m5.986s). No schema migration or database package change was needed; local326/database-contract0.2.26/189-file parity remains current.

Next wire the actual configured handler and CanonicalDurableKnowledgeWorker failure-result branch. The worker currently always calls completeStep for activity returns; captured failure must instead invoke the dedicated failStructuredExtractionStep with its authenticated result. Use readPublished before admission/dispatch and preserve original execution identity with an explicit recovery-build compatibility check. For incomplete operations compose existing registered profile/native admission, pre-reservation execution binding, lifecycle, provider capture/accounting, actual replay, candidate or failure publication and terminal methods. Missing capture after dispatch remains uncertain with no automatic redispatch. Add runtime config/grants, HTTP/client/CLI/MCP and authenticated result reads, configured-worker proof, real process-kill windows and reconciliation before opening default/public admission. All broader module requirements, array evidence, semantic services, human review/calibration, deployment/cutovers and final46-row audit remain active. No full matrix row is closed by this recovery slice.

Evidence in parent `internal/`:

- `verification-structured-extraction-terminal-67ab73e1-7a81-41d7-a690-db1ca794f7d9.json`: SHA256 `cda77fc8640acac4f8272ca39ba401449082a4eb7a7b4c1227204ed4f2909c21`
- `verification-structured-extraction-failure-terminal-5223e5b3-f723-4567-87f6-5e6123f74016.json`: SHA256 `b23a0e2c63970217d9850b474c87f2ad6e2fdbe5b304cd0ec68eb9ad3f70b8ba`
- `verification-structured-extraction-accepted-recovery-audit-20260906.json`: SHA256 `5b22c5b6d4d0ccfe7105618be6d8adc68f3a530c40856cd9715b72d22630852c`
- `verification-structured-extraction-failure-recovery-audit-20260906.json`: SHA256 `6fc18bce73aa3ca084a087342395734e918be069784310b633e458ba0b8fa0dd`
- `verification-structured-extraction-recovery-workspace-20260906.log`: SHA256 `c3e31a2a3863f50f7380936918d11db07f3ea68ae32e7063252d5ad28e0a322b`
