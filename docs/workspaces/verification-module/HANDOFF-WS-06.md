# WS-06 provider framework handoff

**State:** bounded live-safe slice complete; provider promotion remains pending.  
**Owner:** verification-providers-agent.  
**Date:** 2026-09-06.

The provider framework implements Gateway structured extraction and trusted-identity semantic judgment plus Interfaze strict structured extraction and fixed tasks. `AccountedVerificationProviderSink` is the required reusable production composition: it wraps the artifact composer, requires a sink-bound synthetic/public external-processing grant, registers restricted input and exact request bytes, reserves and exclusively fences an immutable tenant-scoped attempt, then retains raw response bytes before HTTP or JSON/schema acceptance. `VerificationProviderArtifactComposer` alone is an internal artifact building block and cannot authorize dispatch. Requests reject redirects and tools, use bounded request/response/depth limits and deadline cancellation, and expose typed failures without upstream strings. Interfaze sends `x-interfaze-zdr: true`; this is an HTTP request property, not a claim that the provider enforced retention.

Raw content uses content-level CAS. Request-specific response envelopes and precontext envelopes preserve parent lineage when bodies or projected precontext repeat. The accounting ledger has ordered locks, idempotent settlement, tenant artifact FKs, RLS/runtime grants, replay identity checks, fencing, retained uncertainty, and true-cost overrun behavior. Actual cost is distinct from estimates and unknown cost; BYOK remains unknown where upstream supplier charges are unavailable.

The shared immutable pilot pointer is `internal/verification-provider-pilot-budget.json`, with the USD 20 cap shared by WS-06 and later WS-07. Do not create another pilot budget. Gateway structured text, Haiku semantic text, Interfaze strict text, and Interfaze OCR have synthetic live evidence but remain **lab**. Interfaze object detection, scraper, speech-to-text, and translate are also **lab** with no live evidence. Human-controlled promotion is required before any state change. No path is admitted for production data.

Live proof first wrote `internal/verification-provider-live-conformance-6659519f-5a87-4422-86a6-cc8455631158.json` (SHA-256 `5ca7e39ae00dfd2b316022271dcada221cd89cf0d1a05fb8d96b7607210f63b7`). Its Interfaze result-shape bookkeeping failure occurred after raw persistence and is retained. The linked no-dispatch reconciliation `internal/verification-provider-live-reconciliation-bc098d95-a789-4bf0-8497-a8f79ab8e8fb.json` (SHA-256 `8591797949da210f56000f5541d7e03fe86719e20bade50b1b11e9b9f18f1419`) proves strict text exact fixture match and fixed OCR shape from the original raw artifacts. Gateway actual-cost reconciliation is `internal/verification-provider-gateway-cost-reconciliation-b6a8fc0e-e6ba-4edb-9096-83dd9665d14f.json` (SHA-256 `96312124e6eab0c4af7012503247f40768d76065459ad34b2dce7eda7fd92d56`). Interfaze costs remain uncertain and reservations remain held.

Independent parser-v2 delta evidence is reproducible with `corepack pnpm exec tsx scripts/prove-verification-parser-v2-visible.ts`; it generated `internal/verification-parser-v2-independent-f9d114b9-2141-463b-9abf-595ed48d6c9d.json` (SHA-256 `b1297ca205ad858287697b2c0ab624a5042d5beaba35fc3d86c6eb84060f5287`) against source image `sha256:1669a3f9674b2e0a70ba1c8686c1cb3647492b8268bdfa7a9a4452fb0530fb37`, with nine visible selectors and both hidden-content checks.

Focused verification passes 66 provider/verification tests and 18 application tests. `corepack pnpm verify` exits 0 with 43 typecheck tasks, 43 test tasks, and 24 build tasks. Immutable final log: `internal/verification-ws06-final-verify-20260906-7c1829c0-56d7-4374-a347-45ea67bfc3f9.log`, SHA-256 `abafa9bad9e1c62aba164d6db5f9fcb9139409fa5c92b73530e09caa44cdb03e`.

Canonical migrations 20260906010000 through 20260906018000 are local-applied without reset. The immutable database-contract vendor archive is `packages/persistence/vendor/aiengineer-database-contract-0.2.6.tgz`, SHA-256 `f1a1f95a588e16988dd3d8fb9361d9e3ed5348ad6d49f25a83d04b985516e482`. Canonical/source parity command is `python ../internal/verification-vendor-parity-review.py`; receipt `internal/verification-vendor-parity-review-2ec5e1a9-6231-4929-ac58-d1e82fea8745.json`, SHA-256 `aa7653d8ea793b58fceef9b17c94351b2baee5c28064361f82011a8426b14ca3`, confirms 89 active migrations plus five TypeScript sources and archived/deferred SQL. Fresh-chain receipt `internal/fresh-chain-audit-providers-0906-final.json`, SHA-256 `e1da8b5181a0fff456d89f5e6f5e93adf987d00583046a141623c52183a96b23`, proves 89 migrations in an isolated stopped container. The parser-v2 probe script SHA-256 is `7ed0c9e546b6a0af87d5da701beb24b5bdb68ac339a2cd2bf3e1967cdefeee4b`.
