# Cursor Cloud integration preparation

Read-only discovery succeeded against the current v1 API, using the existing API key in memory. `internal/verification-cursor-api-readiness-b468dce3-b5c6-440c-a4e1-deb9d25e53d8.json` SHA-256 `d621266511893e8c0db9e50ed8d77909d24d02b9fde2b298332ad4ec37238596` records HTTP 200 for models and repositories. Thirty-six model entries include gpt-5.6-luna, gpt-5.6-terra and claude-haiku-4-5. Three repositories were returned but none matched the four expected workspace repository names. No agent or run was launched, account settings changed, token values printed, or model inference purchased.

The Cursor consumer's origin is git@github-legacy:Overton77/ai-engineer-industry-cloud-automated-research.git. Knowledge Services currently has no origin remote. Do not assume the source-control installation can access a release branch. Do not push the dirty monorepo baseline merely to obtain a cloud test.

Primary API reference checked during preparation: [Cursor Cloud Agents API](https://cursor.com/docs/cloud-agent/api/endpoints). Use v1 agent/run lifecycle and current model parameters rather than old v0 examples. The documented no-repo launch may support a bounded packaged-client HTTP conformance fixture without changing repository installation permissions. That is a possible fixture route, not proof of consumer repository cutover.

For actual WS-10 proof, first finish the public client/CLI and an authenticated remotely reachable test service. Create a bounded cloud run using the economical discovered model, immutable fixture/client identities and canonical tenant/mission/work-item/attempt identity. Cloud tools must invoke the service rather than duplicate verification algorithms. Retain run metadata, client command/output artifacts and the server's durable operation/run manifest; test wrong identity and corrupted evidence alongside the valid request.

Pass a short-lived scoped service credential through an admitted environment/secret mechanism, never in the natural-language prompt or public artifacts. The documented envVars feature may be silently ignored for some accounts; a non-secret existence check must succeed before relying on it. Do not combine envVars with a client-supplied agentId. Handle uncertain create responses without blindly launching duplicates. Agent/run identifiers and streamed status/artifacts are separate in v1. Disable automatic PR creation for a fixture. Preserve evidence and archive only the test agent after completion; do not delete user work.

Read the consumer AGENTS.md and run npm run verify:environment before implementation. Local CLI authentication is already observed; no actual Cloud/service invocation is proved by that readiness check. Repository access, deployed service behavior, consumer cutover and cloud receipts remain distinct acceptance gates.
