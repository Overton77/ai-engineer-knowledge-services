# Offline company fields and policy replay — EV-164 — 2026-09-08

The installed command now retains 19 exact company-field leaves and 38 conservative native policy executions/replays, alongside the existing 40 native claim checks, three report checks and 29 captured report assertion assessments. It produces 33 hash-checked files with credentials removed and fetch disabled. No new provider, Cloud, database or migration calls were made.

## Actual engineering change

The real-source test found that admitted HTML selectors could select a whole DOM statement but could not select a field inside it. An optional `textRange` now selects a half-open UTF-16 range within an already unique DOM target. DOM visibility/uniqueness, whole-statement fallback agreement, immutable projection hashes, bounds and Unicode boundaries remain enforced. Existing selectors retain their original output when no range is supplied. Contract artifacts were regenerated; no persistence migration was required.

Both company plans execute through `VerificationAdmissionService.verifyExtractionWithEvidence` and reproduce the same digest on a fresh execution. TruDiagnostic has 14 leaves in 10 populated categories; Generation Lab has five leaves in two populated categories. Each plan enumerates 47 categories, with 37 and 45 respectively marked unavailable. This is bounded extraction from the selected frozen statements, not complete website coverage or a gold field benchmark.

The field view preserves literal values, numeric ranges and plus-bound qualifiers, units, capture time, exact selectors, and parser/projection/transformation lineage. The 2–4 versus 3–4 week wording and 19 versus 21 system wording remain separate conflict sets. Different algorithms are not treated as conflicts. Product/version facts remain unspecified where the selected evidence does not establish them; capture dates are not product versions.

The policy path reuses the original authenticated semantic assertion IDs and deterministic bundles. It does not rename legacy assertions to match the newer report ledgers. Canonical policy bytes, recorded inputs, evidence metadata and decisions are retained in `policy-replay.json`; replay yields identical decisions. The new definition is explicitly an engineering policy, not historical approval. Outcomes are 25 review and 13 fail. Independent corroboration is false throughout; the scoped promotional/comparative and publication-product overextension cases withhold authority. Unknown applicability, rights, freshness and human authority remain unknown.

The audit now displays captured semantic outcomes rather than lexical substitutes. Available report diagnostics appear beside their selected fragments. `company-fields.html` provides a read-only field ledger with local evidence links.

## Retained installed proof

Receipt: `internal/verification-offline-demo-EV164-fields-policy-final-20260908.json` at the shared workspace root.

SHA256: `a859263417af2e470b98a7fbb46c727c6725af442b50b83db71e9f7c1ac0d3a2`.

The run completed in 37,006 ms with expected exit 2, 33 output files, zero provider requests and no acceptance promotion. Every output hash/length, policy/input digest, replay decision, field qualifier/conflict, and field-to-appendix link was checked. The original EV-163 captures and paid run remain unchanged.

## Validation

- Contract package: 85 tests passed; build and generated schemas passed.
- Verification package: 110 tests passed; build passed. The focused selector/extraction set also passed 26 tests.
- Company-field and policy application tests: nine passed against the final implementation, including real frozen-source admission and fresh field replay.
- API OpenAPI parity: passed.
- Application build/declaration check and CLI build/typecheck: passed.
- Final installed CLI regression: passed in 36.54 seconds; six option/tamper tests passed in the earlier full invocation. Retained pilot regression: passed in 22.63 seconds.
- Independent review found no actionable issue in the native range boundary or policy/root integration.

An initial field test exposed the unsupported standalone text-quote selector and led to the native range fix above. An installed-test assertion subsequently exposed a Windows encoding error in two test literals. The first correction did not match the legacy-encoded bytes; explicit ASCII Unicode escapes corrected them. A display separator in the audit footer was also restored to UTF-8. Final rebuilt installed proof and regression both passed. The earlier 29,823 ms proof remains preserved at internal/verification-offline-demo-EV164-fields-policy-20260908.json (SHA2560048c039c5b6a82a08e391456beda825632cf5633db377c9c973c48e318b314d); it is not overwritten.

## Completion boundary

Acceptance remains **31 proved / 8 partial / 7 missing**. The demo correctly retains its incomplete exit and does not admit reports whose consistency checks fail. Complete structured/provider coverage, the paired quality benchmark, calibration and citation-quality measurements, source/vendor policy approvals, authentic human review and final release audit remain outstanding. These changes do not close the full module or promote human-gold acceptance.
