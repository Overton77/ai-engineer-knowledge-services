# Frozen diagnostics source registry preparation

The coordinator registered all 16 frozen sources in real local Postgres and private Storage, using existing canonical registration and application admission. This is source preparation, not a frozen labeled benchmark, licensed redistribution approval, human gold, provider inference, or accepted report.

- Import ID: `7a74a7a8-0dc4-4591-8bb1-5781ebddef42`.
- Local tenant: `fbfa12cf-0cef-423d-858e-f7a3e213fa80`.
- Manifest artifact: `1203656d-6081-55d8-a707-86657b42e546`.
- Canonical registered manifest digest: `sha256:69b349b3c174466f9dafac4dbca713f269116e43570394eb3943dfceede8af42`.
- Local wrapper receipt: `internal/verification-frozen-source-registry-7a74a7a8-0dc4-4591-8bb1-5781ebddef42.json`, SHA-256 `0f56fca125cb2aef887be7c4b57a63328f549096ee4346ae94687c249865f749`. The wrapper includes the manifest handle in addition to the registered manifest body, so its file digest intentionally differs.
- Log: `internal/verification-frozen-source-import-20260906-r3.log`, SHA-256 `cc0edf6d1bbe8945d63c6cd8a80a0748e015f1dc16d943d924eab306db19cbfb`.

The registry includes the 13 specification captures, rectification publication, real 19-versus-21 article, and actual public TruAge sample PDF. Original Firecrawl JSON bytes are registered before extracting the exact `/rawHtml` string to UTF-8; that derivation has a parent artifact and transformation signature. PDF bytes retain their independently checked capture digest. Every source is restricted and every canonical projection is registered with a checked source/native/projection envelope. All 16 sources admitted, with 17 projections because the PDF has text and geometry. Each resulting envelope was rehydrated and revalidated.

Original recorded capture timestamps are preserved. The supplemental rectification provider response lacks its original timing receipt: `originalCapturedAt` is null and the new capture explicitly describes an import with an unknown acquisition timestamp. Import time is not represented as a historical source retrieval date. Publication dates do not prove a product transition. Rights remain source-specific; no full capture is approved for redistribution by this import.

## Candidate parser image v2 — independent review pending

The first whole-corpus attempt registered all sources but admitted 13/16. The two long papers exceeded the optional aggregate text field cap; the product page exceeded the canonical projection byte cap. Its immutable original registry remains in `internal/verification-frozen-source-registry-905fa617-010e-4a1b-92d5-10ee6bf459a8.json` and the original image remains available.

The revised Python HTML projection keeps a hidden/inert subtree's root placeholder, retaining its outer DOM position, while omitting descendants that were already excluded from selectable evidence. Original bytes remain in the source capture. All visible text nodes remain. For pages over 100,000 UTF-16 code units, the optional duplicate `canonicalText` field is omitted; structural selectors still resolve the full visible DOM. No TypeScript contract or resource ceiling was increased.

Candidate immutable image: `sha256:1669a3f9674b2e0a70ba1c8686c1cb3647492b8268bdfa7a9a4452fb0530fb37` (`aiengineer-verification-parser:v2` is only a local discovery tag). Python source SHA-256: `90775b768424e59540e350e75f1319e92b010f6d8236715b480cbbc425977c34`.

The real corpus passes 16/16 admission. A separate synthetic 2,253,189-byte HTML fixture produces a 162,503-byte projection; all 151 visible fields resolve exactly, omitted inert payloads do not leak into the projection, and the hidden placeholder remains unselectable. Receipt: `internal/verification-parser-v2-projection-proof-20260906.json`, SHA-256 `69df7c36674b2d2effcdeab47c696ba0a80b7485fdc81ac0099e5fe94bcd3ee4`. Page-limit rejection, active PDF cancellation, mixed text order, hidden-content rejection, and job cleanup also pass with v2: `internal/verification-parser-boundary-proof-20260906-v2.json`, SHA-256 `be7ffdc11079c7650e6dd5963adcf2fa64031069591d08e2c2f886c07e295670`.

The next independent owner must review the small Python change and verify visible-text/selector preservation before accepting v2. Previously accepted parser proofs refer to v1 and have not been rewritten.

## Reuse and remaining work

Use the full handles in the registry to authorize and hydrate local Storage artifacts; do not reconstruct metadata from bare hashes. The preparation script is `internal/verification-register-frozen-sources.mjs`, invoked through local-only `internal/verification-run-local-proof.mjs frozen-sources`. The script reads the configured package public build exports. A new invocation creates a new immutable import and tenant; it does not refresh or overwrite the existing dataset.

WS-07 still owns approved short excerpts, case definitions, proposed/adjudicated labels, frozen paired arms, real provider execution, sealed results, statistics, offline artifact packaging, and the four reports. The final replay path must also enforce parser-admission envelopes, as recorded in ADMISSION-REVIEW.md.
# Restricted offline preparation

Coordinator exported the complete authorized parent closure to `internal/verification-offline-source-preparation-9093b465-16c2-4cc1-b7f4-728d3958b0ed/`: 82 byte-verified registered artifacts, 16 registered source/capture records and 17 projections re-admitted through the application envelope path. No parser invocation or source reacquisition occurred. Canonical `manifest.json` SHA-256 is `50a3552cffc639a78d7789865be929baefd64d2342431e2de958e669c21f163e`. `source-registry.json` preserves the original wrapper; artifact files are named by registered UUID and the manifest retains complete handles. Harness `internal/verification-export-frozen-sources.mjs`, invoked through local-only wrapper mode `frozen-export`, refuses overwrite and validates the registry wrapper/canonical hashes before hydration. This is restricted local preparation, not an approved redistribution or human-labeled benchmark. Parser v2 independent review remains assigned to WS-06.
