# Atomic original-attempt reconciliation

EV-093, coordinator review, 2026-09-06. The admitted operator decision can now be applied atomically to an original provider attempt after canonical failure or cancellation. The full verification mission remains active.

PostgresProviderReconciliationStore requires the original per-instance admission permit, checks expiry again after acquiring the terminal operation lock, and returns an existing exact ledger entry for matching retries. Different artifacts or decisions for the same attempt fail. It inserts one record into orchestration.verification_provider_reconciliation; the native trigger performs the reservation release and actual-cost settlement in the same transaction. The result exposes compact receipt identity and accounting amounts, never redispatch authorization.

Migration328 adds the append-only ledger, full registered-handle matching, original-identity and execution-mode validation, canonical receipt/payload hashes, native artifact ancestry/signature binding and atomic application. The original operation is locked first, then budget and provider. Only control_plane may insert ledger records; ordinary worker roles cannot. The provider claim guard admits only the exact transition corresponding to the newly inserted ledger. It requires all provider fields except state, actual cost and reconciliation time to remain identical, including original response and dispatch identity. Ordinary live-worker settlement rules remain intact.

The initial native proof exposed migration140's requirement for a response envelope on settlement. Follow-up migration329 preserves that ordinary rule and adds only the exact ledger-authorized transition, allowing a missing response to remain missing. No response evidence is fabricated. Applied migrations were not rewritten. Local329 and database-contract0.2.28 are current; canonical/vendor/installed parity covers192 files. No remote migration was applied.

The final preparation creates two fresh attempts on an isolated 200-micro-unit synthetic budget. Both child workers are killed after dispatch and before returning a response. Gateway is recovered into failed/uncertain; Interfaze is cancelled while the original accounting remains dispatched. The final signed settlement proof applies300 to Gateway and0 to Interfaze. All eight grouped checks pass: admission does not mutate accounting, forced post-insert rollback restores provider and budget rows, executor_service insertion is denied, concurrent exact retries return one entry, conflicting decisions fail, and ledger mutation is rejected. Both original response references remain null and dispatch identities remain unchanged.

The independent audit verifies eight actual Storage artifacts and both operator signatures, exact provider/ledger/evidence bindings, four current source hashes, five installed SQL function bodies against canonical migrations and control-plane tenant isolation. The final isolated budget has zero reserved,300 settled and200 ceiling. An actual subsequent reservation attempt fails with PROVIDER_BUDGET_EXCEEDED. This overrun is visible and is not discarded to satisfy the ceiling. Preliminary proof cohorts remain separate historical test data.

Evidence in parent internal/:

- verification-provider-reconciliation-2520a59b-d608-4d8c-bd48-43d02830fa1d.json — SHA256 83ac4b904cd62ac18ffa7e62f67c136ad3079ba0ef92661da7ce83c548f41a29.
- verification-provider-reconciliation-audit-20260906.json — SHA256 61ec880375ff2e00d2e61c41805f2e07080583bf56cb97d2e19a7fe28895ea4c.
- verification-provider-reconciliation-workspace-20260906.log — SHA256 84e5c82e1d08b77ed07aea604a320dddb3ee3fc920455ac0572e8745b2215682;72 tasks passed uncached in2m7.79s.
- verification-contract-0228-audit-20260906.json — SHA256 3dcbe342df5a22465e6afb994c64698ae2656c36677882a220ef964c3d4d6d2d.
- Final preparation: verification-provider-reconciliation-preparation-30cc6952-5f73-41d6-b647-a0547c2e3d40.json. Proof TypeScript and DB generated-type/type checks passed.

Reproduction: run local helper provider-reconciliation-prepare, then set VERIFICATION_RECONCILIATION_PREPARATION to its fresh output filename and run provider-reconciliation. Settlement proofs consume their prepared attempts; do not rerun against an already settled cohort. Audit helper provider-reconciliation-audit takes VERIFICATION_EXTRACTION_WORKER_RECEIPT. Never load the remote KS .env for local tests.

Limits and next: this is an internal control-plane persistence boundary with synthetic evidence, not deployed operator transport or supplier billing proof. It currently accepts failed/cancelled operations only. Successful operations with unresolved provider cost still need reconciliation support. Add that terminal case and prove that completed success/failure publications remain byte-identical and readable after accounting settlement, then wire operator authorization/runtime and test broader cancellation/partial-retention cases. Exact application retries require a still-valid original admission permit; a later read of expired applied decisions needs a separate authorized read boundary. All semantic, case/score, replay/export, deployment/security, runtime-cutover, dashboard, human-review and final-matrix requirements remain open.
