# WS-08 structured extraction failure terminal review

2026-09-06 UTC

EV-085 accepts exact terminal completion for signed captured structured-extraction failures. This extends EV-084 custody; configured worker/transports and full-module acceptance remain unfinished.

`VerificationStructuredExtractionFailureResultSchema` defines the compact strict result: operation/use-case/request digest, failed status, native code and derived category, automaticRetry false, null candidate, execution reference, manifest/provider-call digests and full failure artifact handle. `createStructuredExtractionFailureOperationResult` snapshots caller state before awaiting, verifies canonical body digest, canonical64-byte Ed25519 signature, provider-call digest, full artifact bytes/size/time/ordered parents and native failure transformation signature, and returns a frozen result. A final review added standalone code/category consistency to the Zod schema. The generated JSON Schema captures shape but cannot encode this runtime refinement; application parsing and SQL both enforce the derived category.

`PostgresCanonicalRepository.failStructuredExtractionStep(tenantId,lease,{id,idempotencyKey,executorIdentity,output})` locks the operation first, reconstructs the DB-owned expected result and compares its canonical hash before mutation. It checks exact step input and live holder/token/fence, creates the transaction-local failure terminal claim, marks the step failed, releases the lease, appends the guarded event/outbox, inserts the immutable extract_and_register.failed receipt and reconciles operation state in one transaction. Exact idempotent reuse returns the original receipt; executor or payload drift rejects without a new terminal mutation. Generic failStep snapshots inputs and follows operation-first locking for structured extraction; once a captured failure checkpoint exists, it rejects both generic retryable and nonretryable failure calls.

Additive local326 independently reconstructs the expected result/full artifact metadata, correlates receipt/output hash/executor/input/event/lease/original publication, and requires one exact step and receipt for failed operation completion. A deferred constraint rejects an orphan failed step at transaction end. Any queued transition after a captured failure checkpoint is prohibited. Direct SQL cannot replace this chain with a generic failure receipt. Generic infrastructure failure before the captured-failure checkpoint retains its existing semantics, and cancellation remains available. Existing success guards and immutable input/execution/ownership/candidate exclusion remain intact. Applied migrations were not edited.

The final real local proof passes25 checks: four original Gateway/Interfaze synthetic calls cover HTTP503 and HTTP200/schema failure. It retains and signs captured failures, naturally expires leases, recovers original custody under higher fences, and completes all four actual operations and their sole steps as failed with exactly one receipt each. Wrong holder/input digest, extra fields, automaticRetry true, candidate insertion, artifact substitution, generic failStep retry/nonretry, direct failed-step/requeue writes and orphan terminal transaction fail closed. Exact canonical SQL/body/hash equality and idempotent receipt reuse pass. Each final proof call remains uncertain at ordinal0 with null actual cost and100 micros retained liability (400 micros for the final four calls). No supplier calls, actual billing, process-kill recovery or full historical-budget accounting is claimed.

An independent native audit downloads32 Storage objects; validates exact full handles, hashes/size, Ed25519/tamper rejection, failure/execution/parent/provider bindings, fourteen retained source files and eight installed SQL bodies; and checks four exact failed receipt/event/outbox/lease/operation chains. Tenant app_reader isolation passes; raw receipt-schema access remains denied. A bounded static review by the implementation agent of the separately owned root persistence/SQL found no issue; root also reviewed the helper and independently audited the runtime artifacts and SQL. The prior separate review agent could not be resumed because the tool reported its thread limit, and no independent-agent claim is substituted for the actual audit.

Contracts46 tests, application196 tests, standalone proof typecheck, database typecheck/current generated types and all72 uncached workspace typecheck/test/build tasks pass (2m9.774s). Local database-contract0.2.26 matches189 canonical shipped files and its installed vendor package. No remote migration ran; remote remains at the previously recorded220 boundary. Existing opt-in skips are not counted as runtime evidence.

Initial terminal proof ff0e24f1-bd09-4c32-980c-09e877e2437e passed before the final standalone code/category refinement; it is retained as historical evidence and its four real failed operations and unknown-cost reservations remain. Final proof c23a8ffa is authoritative for current source. Initial audit failures reflected source drift and audit-script declaration/signature-prefix mistakes; those were corrected before the successful independent final audit. No production behavior was weakened to satisfy the audit.

Next implement the configured extractStructuredData executor/worker with registered runtime admission, original pre-dispatch execution binding, actual provider capture/accounting, accepted-versus-failed branches, publication recovery and the canonical success/failure methods. Preserve a committed publication's original accounting snapshot even if later reconciliation changes provider state; hydrate and authenticate original artifacts during recovery. Add HTTP/client/CLI/MCP and authenticated result reads, actual process-kill windows, provider crash-gap/reconciliation semantics and recovery-build compatibility. Public/default admission remains closed until configured runtime proof. Array evidence, verifyClaims/verifyReport/adjudication, canonical case/score completeness, full disconnected replay, security/deployment/runtime cutovers, dashboard/human review/calibration and the46-row acceptance audit remain open. No entire acceptance row is closed here.

Evidence in parent `internal/`:

- `verification-structured-extraction-failure-terminal-c23a8ffa-3c3b-42dd-840b-6f04b6ead67f.json`: SHA256 `7b3b520a5007b9702b6400c6d8e9a56d4341a25b5179d4406a10ffbb4a6bd4e0`
- `verification-structured-extraction-failure-terminal-audit-20260906.json`: SHA256 `b0303d548e2d5b9dd479d2eef08ff93a2b7acb248141ee2e4351a3cc224efff6`
- `verification-structured-extraction-failure-terminal-workspace-20260906.log`: SHA256 `f7ae0e70192e290e9601bb7b60c1c40367ab05513e675117ba313f28996506d8`
- `verification-contract-0226-audit-20260906.json`: SHA256 `cea0016b77b05abf011e1ae6dccdaeb3bdc9b6aee0d238fe2388081f0ecee007`
