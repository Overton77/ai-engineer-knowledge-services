# Structured extraction executor continuation

The complete verification-module goal remains active. These prerequisites do not implement the public extraction operation.

The reviewed profile admission is exported from application. Runtime grants and the producer's extraction-schema reference now require full registered handles, while public requests remain compact. Preparation is trusted only by its issuing admission instance and retains profile/schema/source/transformation handles. It supports fixed object schemas with exact leaf/rule/evidence coverage; arrays and open objects fail closed. Provider registry `lab` status is retained and does not authorize promotion or dispatch.

Canonical local migration `20260906031300` and database-contract0.2.16 add operation-scoped provider accounting. Instantiate `PostgresVerificationProviderAccounting(database, { lease, profileArtifactId, profileDigest })` using the actual leased structured-extraction operation. The existing UUID `dispatchFence` remains a uniqueness marker; live step/token/holder/canonical fencing is authority. Stable logical provider attempt IDs must derive from the operation and call ordinal, without salting the actual wire digest or inventing an orchestration attempt. Initial reserved and actual dispatch fences are distinct immutable history. Replacement workers reuse the same row and cannot redispatch uncertain/dispatched/settled calls.

Next implementation, in dependency order:

1. Add `verification_structured_extraction` / `extract_and_register` to shared operation composition while keeping default admission closed until the executor/runtime is configured. API grants must match worker tenant/capture/representation/schema/profile grants exactly.
2. Retain transport status with the actual response before output interpretation. Existing `verification_provider_response_envelope.v1` contains request/raw bindings but no HTTP status; do not infer status from raw JSON or rewrite existing envelopes. Design a dedicated operation-bound dispatch checkpoint referencing the existing admitted envelope and exact status, plus any parser failure code and provider precontext custody. Recovery must use the real adapter in memory, with no external retry.
3. Compose the actual registered Gateway/Interfaze adapters with branded profile preparation, operation-scoped accounting, bounded request/response retention and cancellation. Lab registry membership is not deployment approval. Runtime configuration must explicitly select any admissible execution mode and source-processing authority; unavailable or unconfigured profiles fail before operation admission. No synthetic provider response can become a live-provider claim.
4. Register the candidate output and immutable extraction-result manifest with exact request/profile/schema/capture/projection/transformation/provider-response ancestry. Schema validation establishes only candidate shape. Output remains unverified; deterministic/semantic verification is a separate use case. Do not fabricate a verified seal for producer completion.
5. Add canonical durable candidate/result timing and terminal guards, recovery after reservation, after dispatch with no response, after response retention, and after candidate registration. A dispatched row with missing response is uncertain and requires reconciliation, never automatic redispatch. Post-cancellation settlement currently fails closed and retains reserved liability; a separately authorized, audited reconciliation path is still required before claiming this lifecycle complete.
6. Add configured worker and HTTP/client/CLI/MCP routes, compact authenticated reads, generated contracts, actual local adapter execution and canonical completion proof. Then use separate OS processes for dispatch/response/candidate crash windows. Tests using synthetic transport must say so. Paid/provider deployment evidence is separate.

Root-reviewed accounting evidence is in the EV-077 handoff/ledger. The proof uses real local PostgreSQL/Storage and synthetic accounting records; it does not admit a real producer profile, dispatch a provider, run extraction, or establish supplier billing. Other mission requirements (claims/report/adjudication, canonical case/score completeness, disconnected replay, runtime/deployment cutovers, dashboard/human review and full matrix audit) remain unchanged.


Continuation update after EV-079: status capture and actual captured-byte adapter replay now exist and have named live local proofs. See WS-08-STRUCTURED-EXTRACTION-REPLAY-REVIEW.md. The next implementation is durable candidate/result publication, canonical operation lifecycle/terminal guards and configured executor/worker/transports. Do not restart the completed transport/replay foundations. Profile admission's resolver parameter is now a factory, not a shared resolver instance.


Continuation after candidate retention: application now has issuing-instance replay result branding and `StructuredExtractionCandidateBuilder.retain`. Actual CAS is tenant+payload digest; candidate/precontext bytes are therefore operation-bound versioned envelopes. The candidate envelope has the extracted `output` and separate `outputDigest`; the artifact digest covers the full envelope. Precontext envelope preserves exact emitted bytes as base64 plus their digest. The provenance artifact binds profile/schema/source/projection/transformation/transport/envelope/request/raw and optional precontext, and all output status is `unverified_candidate`.

Next canonical integration must initialize immutable operation identity and original timestamps under the live lease before calling the builder. Production must not use the proof's deterministic capture+1000ms audit timestamp. A canonical row must bind actual operation/step/provider attempt/profile/request/capture and exact candidate/provenance hashes, ordered parents and the exported pipe-hash metadata binding. Persist completion/publication with canonical times, then exact success receipt; failure results need their own durable no-candidate outcome. Migration315 intentionally denies structured-extraction step and operation success while this lifecycle is absent. Replace its guard only with the reviewed lifecycle/receipt checks, never remove it merely to run the worker. Migration316 adds the dedicated scoped precontext type. Local DBcontract0.2.19 supersedes the intermediate unaccepted0.2.18 package.

The current proof replays previously cancelled synthetic-provider captures solely to retain offline audit candidates. It does not revive those operations, mutate accounting, establish a live dispatch grant or complete `extractStructuredData`. Configure actual executor/worker/transports and authenticated reads only after canonical completion exists. Missing capture after dispatched provider row remains uncertain/no automatic redispatch. Array evidence, reconciliation, process-crash windows and every other full-module gap remain open.


Canonical candidate checkpoint continuation: migration317 and `PostgresStructuredExtractionLifecycleStore` now implement `initialize({identity,lease})`, `beginRetention({lifecycle,lease,capture})`, and `completeRetention({lifecycle,lease,candidate})`. Identity includes DIFFERENT operation request and step input digests; use actual `PostgresKnowledgeOperationService` envelopes, never force those hashes equal. The extraction kind is verification-owned for authenticated context/attempt storage, but default admission remains closed. Initialization returns existing states under a replacement live lease. Statuses are running -> retaining -> retained; startedAt, retentionStartedAt, and completedAt are canonical DB times. Candidate builder uses retentionStartedAt. Capture checkpoint works when the provider response reference is still NULL before accounting settlement. Candidate/provenance objects are checked against exact canonical payload bytes and stored registry hashes. The table does not store a separate unbound outputDigest column; hydrate the admitted candidate/provenance for that value.

Next required implementation is final result publication and exact success receipts (and durable provider-failure outcomes). Extend the reviewed canonical lifecycle rather than bypassing migration315's success denial. A final immutable result must bind operation/step/producer attempt, original provider capture/fence, request/profile/schema/source/candidate/provenance, original runtime/code identity and canonical times. Its output must explicitly remain unverified_candidate. Store publication under the active lease with semantic metadata binding, then replace the temporary terminal guard with exact publication/receipt checks for both step and operation. Keep failure outcomes separate from accepted candidate completion. After this, configure the actual executor/worker and HTTP/client/CLI/MCP/authenticated reads, then process-kill windows and reconciliation. Current replacement proof uses natural lease expiry; it is not SIGKILL evidence. All other mission gaps remain active.

Final lifecycle review accepted as EV-081; local317/database-contract0.2.20 current. Before final result publication, extend canonical initial identity with immutable runtime/code provenance (the current checkpoint is not a complete run manifest). Keep capture/candidate recovery times intact and retain original provider fence separately from the replacement lease fence.


EV-082 continuation: execution identity and custody publication now exist. Use `StructuredExtractionExecutionArtifactBuilder.prepare` and `PostgresStructuredExtractionExecutionStore.bind` before ANY provider reservation; original execution is separate from the immutable317 lifecycle identity. `readForRecovery({lease})` retrieves its stable metadata. Dirty code requires canonical listed-file source custody under its dedicated type; never substitute parser-container identity for worker-container identity. `StructuredExtractionPublicationArtifactBuilder.publish({lifecycle,executionArtifact,providerCall})` signs the exact retained candidate with explicit unverified/shape-only status; `PostgresStructuredExtractionPublicationStore.publish({lease,manifest,artifact})` authenticates and commits it. Provider call state/actual cost are snapshot custody; SQL rejects a stale snapshot if accounting changes before insert. A committed snapshot remains immutable and recovery must preserve it rather than regenerate with later accounting state. Local318-320/database-contract0.2.22 are accepted.

Next is the exact success receipt and terminal guard for both operation/step, plus durable provider-failure outcome semantics. Do not remove315 without its reviewed replacement. Current proof25checks/27artifacts/72tasks covers actual lock races and natural replacement leases; it is not process-kill or public-route evidence. Add canonical publication recovery/reads as needed for the configured executor. Keep all wider module gaps active.


EV-083 continuation: canonical success receipt is implemented. Use `createStructuredExtractionOperationResult({manifest,artifact,verifier})` on the committed publication, then canonical `completeStep` with `extract_and_register.succeeded`.321 replaces315's temporary denial in-place: SQL reconstructs the exact result/fullhandle and canonical hash, checks the live terminal claim/event/receipt, and defers the no-orphan-step check to transaction end.322-324 freeze bound input, ownership, creation/retry identity and step membership. Original source files are not rewritten. Local0.2.24/187-file parity and33-check terminal proof/two actual succeeded operations/27-artifact native audit/72-task verification are accepted.

Next implement durable captured provider-failure outcomes (HTTP/schema, explicitly no candidate) and exact failed publication/receipt custody. Do not turn failed replay into a candidate or automatically retry a dispatched call. Preserve current success boundary and its original publication/accounting snapshot. Then configure executor/worker, mutation transports and authenticated result reads; app_reader intentionally has no direct knowledge_service receipt access. Add process-kill recovery and reconciliation. On recovery, retain original dispatch/code identity while checking compatibility with the actual recovery implementation, rather than simply attaching a different build identity. The full module inventory, array evidence, human review and other listed gaps remain active.


EV-084 continuation: captured failure publication is accepted. `PostgresStructuredExtractionFailureStore.initialize({lease,lifecycle,code})` fixes immutable completedAt and returns preparing/published checkpoint. `StructuredExtractionFailureArtifactBuilder(replayService,{artifacts,createResolver,signer}).publish({tenantId,operationId,providerAttemptId,preparation,replay,lifecycle,completedAt,executionArtifact,providerCall})` accepts only actual issuer-branded failed replay. `failureStore.publish({lease,lifecycle,checkpoint,manifest,artifact})` authenticates and publishes under a live claim. Keep the lifecycle retaining, candidate/precontext/provenance null; never fabricate a candidate for failure. Local325/0.2.25,17checks/32artifacts/72tasks accepted. The actual proof covers HTTP503 and schema200 for both adapters; the four-code taxonomy does not imply every code has live captured proof.

Next boundary: exact FAILED result and canonical receipt. Existing generic `failStep` only records `{errorClass,retryable,attemptsExhausted,fencingToken}` with no outputSha256/result artifact or eventId; it must not be used to claim captured-failure completion without new binding. Snapshot caller values before awaits, lock operation first, validate exact holder/step input, require signed published failure for captured calls, reconstruct canonical failure result in SQL, hash exact output, correlate immutable receipt/event/outbox/lease and deferred terminal step/operation. Reject retryable/requeue for captured failures; verify direct SQL cannot bypass. Keep valid pre-dispatch infrastructure failure separate and preserve cancellation behavior. Existing generic cancellation leaves running step rows with released leases; the EV-084 proof explicitly records this and has no succeeded/failed receipt. Do not mistake proof cleanup for terminal-failure evidence.

After terminal failure, implement configured executor/worker, HTTP/client/CLI/MCP and authenticated reads; recovery must hydrate the committed original publication/accounting snapshot even if later cost reconciliation changes ledger state. Add actual process-kill windows and reconciliation. Default/public admission remains closed. Continue the full module inventory, array evidence, semantic/authority services, human review and all previously listed gaps.


EV-085 continuation: exact failed terminal boundary is now implemented. `createStructuredExtractionFailureOperationResult({manifest,artifact,verifier})` authenticates the committed no-candidate failure; `database.failStructuredExtractionStep(tenant,lease,{id,idempotencyKey,executorIdentity,output})` completes its exact receipt chain. Use this dedicated method rather than generic failStep once a failure checkpoint exists. SQL326 forbids requeue and requires exact publication/claim/result/receipt/event/lease plus deferred complete transaction. Generic failStep locks operation first for structured operations and refuses checkpointed failures. Local0.2.26/189-file parity,25checks/four failed ops/four receipts/32Storageobjects/eightSQLbodies/72tasks accepted. Success completeStep remains unchanged. Final proof c23a8ffa supersedes initial ff0e24f1 source evidence; both sets of provider liabilities remain real historical state.

Next compose configured executor/worker and result reads with existing profile admission, pre-reservation execution store, capture/accounting, replay/candidate or failed publication and canonical terminal methods. Existing runtime patterns: apps/worker/src/verification-benchmark-comparison-runtime.ts and verification-benchmark-comparison-activity.ts; application comparison/runtime-config and registered profiles. Add actual configured proof before opening default/public admission. Recovery needs stored full publication handles and original manifest snapshot, not reconstruction from later accounting rows; both success and failure publication stores currently lack complete ready-to-use read APIs for this worker composition. Verify recovery code compatibility separately from retained original execution identity. Continue all broader module requirements and actual crash/reconciliation work.


Recovery composition implementation (pending EV-086 final workspace checks): PostgresStructuredExtractionRecoveryStore.readPublished({lease,signal?}) now returns undefined before publication or a discriminated accepted/failed original manifest, artifact and authenticated canonical result. It authorizes a fresh verification_replay resolver, limits publication bytes, checks full metadata/native hashes/canonical JSON/trusted seal and rechecks operation/lease after I/O. It deliberately does not rebuild providerCall from current accounting. Real accepted recovery35checks/27artifacts and failure recovery29checks/32artifacts have passed; ledger acceptance follows workspace validation.

Worker integration decision for next implementation: CanonicalDurableKnowledgeWorker currently calls completeStep for every activity return and generic failStep for exceptions. Add a narrow extract_and_register + strict failure-result branch to call failStructuredExtractionStep; a captured failure is a durable result, not a generic thrown retry. SQL still reconstructs and authenticates exact committed custody, so shape alone cannot authorize terminal failure. Preserve normal success and unrelated activities. Configured activity must readPublished before preparing or dispatching, check current runtime compatibility with retained original execution, and return its original result when present. For incomplete runs, read original execution before reservation; then profile admission/lifecycle/capture/accounting/replay and accepted/failed retention. A dispatched attempt with no capture stays uncertain; do not invent a response or issue another call. Retryable infrastructure recovery must retain original provider attempt/call ordinal.

Runtime config must resolve registered_default without ambiguous grants (same capture/representation/schema cannot route to two providers implicitly), authorize verification_admission and verification_replay only for the configured tenant, use a server-owned signing key and trusted public-key set, and separate native-parser image identity from worker code/runtime identity. Initial recovery compatibility can require exact registered runtime digest; an explicit bounded compatibility allowlist is preferable to silently replacing the original runtime after an upgrade. HTTP/client/CLI/MCP/authenticated reads and public capability readiness follow actual configured-worker evidence. Full mission scope remains unchanged.


EV-086 final acceptance: publication recovery above passed all72 uncached workspace tasks (2m5.986s), accepted35checks/27Storageobjects and failed29checks/32Storageobjects with independent native audits. Next action is configured extraction activity/runtime plus CanonicalDurableKnowledgeWorker strict failure-result branch, then transport/authenticated reads and actual process recovery. Do not spend another turn restating this recovery plan; use the implemented store in the worker composition. All broader module gaps remain open.


EV-087 continuation: configured worker factory is implemented in apps/worker/src/verification-structured-extraction-runtime.ts and registered by normal index.ts startup. Config env VERIFICATION_STRUCTURED_EXTRACTION_CONFIG_JSON contains schemaVersion verification-structured-extraction-runtime.v1, tenantId, providerId, grants[], runtime, parserImageDigest, executionMode and trustedPublicKeys. Signing key envs VERIFICATION_STRUCTURED_EXTRACTION_SIGNING_KEY_ID / VERIFICATION_STRUCTURED_EXTRACTION_SIGNING_PRIVATE_KEY_PEM; live keys AI_GATEWAY_API_KEY or INTERFAZE_API_KEY. Synthetic mode requires an injected syntheticFetch port (ordinary startup has none). Recovery requires exact runtime digest/mode. Canonical worker now sends strict failed extraction outputs to failStructuredExtractionStep. Accounting.readOriginalAttempt is scoped/fenced and uses ordinal0. Final worker proof97976363 has12checks/six one-call cases/six replacement recoveries; audit44objects;72tasks pass. Source custody covers seven listed files. No new DB migration.

Next action: implement public mutation/read composition, not another worker skeleton. Existing transport owners: apps/api/src/server.ts and index runtime wiring, packages/client-typescript/src/client.ts, apps/cli/src/commands.ts, apps/mcp/src/index.ts. Comparison mutation/read patterns are present. Server verificationContext currently permits six other use cases and must add extractStructuredData only with explicit server-owned runtime grant/readiness admission. Terminal read repository must authenticate receipt/output/native publication, enforce tenant/owner, sanitize errors and expose bounded original results; EV-086 readPublished is active-lease internal recovery only, not the terminal public read API. Keep raw knowledge_service receipt access denied to app_reader. Follow with real process-kill, supplier/capture reconciliation and the full module acceptance inventory.


Continuation after EV-088: terminal internal reads now exist in PostgresStructuredExtractionReadRepository.loadVerifiedExtraction. Reuse this path for public projection; do not use lease-only recovery for terminal reads. This interface returns full manifests, so application projection must remove Storage object paths and enforce authenticated caller scope at the transport. Both accepted and failed outcomes are evidenced. Next complete compact resource contracts/application service/read runtime and extraction mutation admission plus API/client/CLI/MCP composition. All broader acceptance remains unchanged.


Continuation after EV-089: compact resource/application projection, authenticated read runtime, mutation grant parsing and HTTP/client/CLI/MCP paths now exist. Reuse these implementations. Public API configuration requires live_provider mode, dynamic ownership grants and valid Ed25519 trust; synthetic mode is admitted only by explicit local test buildServer configuration. Next integrate fresh public submission through configured worker completion with current scoped source custody, then separate-process provider crash windows and reconciliation. Five final transport-proof submissions were cancelled while queued; they are not worker-completion evidence. Production capability health and all broader mission acceptance remain open.


Continuation after EV-090: public submission now has fresh configured worker completion/read evidence and six actual post-publication/pre-terminal child SIGKILL recoveries. Reuse scripts/prove-verification-structured-extraction-process-recovery.ts and its child harness. Next cover dispatch before durable response capture, partial response/candidate retention, cancellation and authorized uncertain-call reconciliation; the existing recovery must never redispatch a dispatched/uncertain original call. Production package source is unchanged from EV-089; final proof retains17 current source files. Full broader mission scope remains open.


Continuation after EV-091: actual dispatch-without-response child process-kill recovery now has evidence for both providers. Worker marks dispatched accounting uncertain under the replacement lease and fails without retry or public extraction publication. Next implement separately authorized original-attempt reconciliation after failed/cancelled operations; current ordinary scoped settle remains blocked once operation authority ends. Preserve reservation until evidence justifies adjustment, original dispatch identity, immutable publication snapshots and no redispatch. Partial response/candidate retention, cancellation windows and all broader mission scope remain open.


Continuation after EV-092: use VerificationProviderReconciliationSchema and ProviderReconciliationAdmission; do not invent an unsigned accounting override. Admission returns deeply frozen per-instance permits after signature/native execution/request/billing checks and<=24h validity. Next add atomic reconciliation ledger/store and SQL guards. Recheck exact original tenant/op/step/attempt/budget/provider/model/dispatch/request/execution, original liability state/reserve, issuer/expiry and dedicated registered receipt type under operation-first locking. Preserve original response handle and publication snapshots, atomically settle original reservation with overrun visible, exact-retry idempotency, and no redispatch. Local327/contract0.2.27 current; no terminal settlement implemented yet.


Continuation after EV-093: PostgresProviderReconciliationStore and local328/329 now apply admitted decisions atomically with immutable ledger and exact original identity; control_plane-only inserts, concurrency/rollback/conflict/overrun proofs pass. Next expand terminal authority to successful operations with unresolved provider cost and prove existing success/failure publication bytes/public reads stay unchanged after reconciliation. Then configure operator authority/runtime/read/transport. Current store/native guard accepts only failed/cancelled. Use fresh isolated provider-reconciliation-prepare output for settlement tests; already settled cohorts are not reusable. Local329/DBcontract0.2.28 current; all broader mission remains open.


EV-094 extends original-attempt reconciliation to succeeded extraction operations. Four fresh accepted/HTTP-failure cases across both providers pass20 grouped checks; original signed publications, authenticated HTTP resources, operations and receipts remain unchanged after settlement. Independent18-artifact/five-SQL-body audit passes, as do all72 workspace tasks and193-file contract parity. Local330/DBcontract0.2.29 current; remote unchanged. Operator runtime/transport and historical applied-decision reads remain open. See WS-08-PROVIDER-RECONCILIATION-PUBLICATIONS-REVIEW.md.


EV-095 adds internal historical applied-decision reads with real Storage/signature verification at durable application time and a second native ledger snapshot. Four existing settlements pass16 grouped checks, including expired-read/mutation separation, tenant scope, corrupt evidence and injected ledger drift. Dedicated proof TypeScript passes. Local330/contract0.2.29 and remote unchanged. Operator authorization/runtime/transports and broader acceptance remain open. See WS-08-PROVIDER-RECONCILIATION-READS-REVIEW.md.


EV-096 adds configured actor/issuer and mission/deployment authorization, HTTP POST/GET reconciliation, generated contracts and typed client methods. Four fresh settlements pass24 grouped checks through actual HTTP; independent18-artifact/10-source/five-SQL-body audit passes. Original publications remain unchanged. Local330/contract0.2.29 and remote unchanged. CLI/MCP, deployed configuration and broader acceptance remain open. See WS-08-PROVIDER-RECONCILIATION-HTTP-REVIEW.md.


EV-097 adds reconciliation apply/show CLI commands and MCP apply/get tools. Four fresh settlements through built CLI processes and actual MCP Streamable HTTP pass24 grouped checks, plus independent18-artifact/12-source/five-SQL-body audit. All72 workspace tasks pass,66 cached. Local330/DBcontract0.2.29 and remote unchanged. Partial-retention/cancellation recovery, deployed configuration and broader acceptance remain open. See WS-08-PROVIDER-RECONCILIATION-TRANSPORTS-REVIEW.md.
