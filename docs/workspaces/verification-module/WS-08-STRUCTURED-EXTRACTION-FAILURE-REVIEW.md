# WS-08 structured extraction captured failure review

Date: 2026-09-06 UTC

EV-084 accepts the bounded captured structured-extraction failure publication path. It does not complete the extraction service or the module.

The new shared failure schema admits only four native producer error codes and requires a retaining lifecycle with null candidate, precontext and provenance. `StructuredExtractionFailureArtifactBuilder` checks the original replay issuer brand before cloning or awaiting, rejects accepted/forged/mutated replay, freshly hydrates the registered profile, original execution, listed source custody, schema/native source chain and captured response, and signs canonical custody-only bytes. The failure carries an explicit null candidate and `automaticRetry: false`. Its native pipe signature binds the failure code, execution, original dispatch fence, provider-accounting snapshot, original lifecycle times and exact ordered 10/11-parent ancestry.

Local migration325 adds the immutable preparing/published failure checkpoint. Initialization fixes the DB-owned completion timestamp before artifact registration; publication can then recover the same timestamp and CAS identity. The persistence adapter authenticates the server-owned Ed25519 signature, validates canonical payload/identity/metadata, locks operation before live step/lease and compares the original execution and retaining lifecycle. SQL independently checks the actual provider row, dedicated artifact type, hashes, producer, timestamps, parents and native transformation signature. Once any failure checkpoint exists, lifecycle candidate retention is prohibited. Different failure identity, publication replacement, stale lease or changed accounting snapshot cannot commit. The original accounting digest covers DB accounting facts; configuration is carried by the signed manifest and hydrated immutable registered profile. Independent review reconsidered and closed its initial configuration candidate on this explicit signing-authority boundary; no actionable finding remains.

The real local proof passes17 checks across Gateway and Interfaze with HTTP503 and HTTP200/schema-invalid responses. Four original calls use injected synthetic transport; actual adapters throw only after capture custody is committed. Actual captured replay returns the original failures. The proof checks candidate-builder rejection, immutable failure timestamp/identity, direct SQL candidate exclusion, real dispatched-to-uncertain accounting drift, untrusted/forged seals, checkpoint drift, natural higher-fence recovery, immutable published identity and denial of redispatch for the original call. All four provider attempts remain uncertain at ordinal0 with unknown actual cost and100 micros reservation each (400 micros retained liability). No synthetic zero-cost settlement or supplier billing claim is made.

The independent audit downloads32 actual Storage objects and verifies complete registered handles, SHA/size, canonical manifest bytes, native Ed25519 including a tamper negative, all ordered parents, native execution/failure/accounting signatures, original capture/provider/lifecycle/execution/failure rows, ten retained source files and four installed SQL function bodies. Same/foreign-tenant app_reader isolation passes; direct receipt access remains denied. The proof cancels all four operations during cleanup and releases their leases; existing generic cancellation leaves their step rows running. There are no terminal failed/succeeded receipts for these proof operations. This is recorded rather than presented as failed-operation completion.

Contracts44 tests and application193 tests pass. The standalone proof typecheck, database typecheck/current generated types, all72 uncached workspace typecheck/test/build tasks (2m8.684s), and database-contract0.2.25 canonical/vendor/installed parity across188 files pass. No remote migration was run; local schema is through325. Tests skipped by their existing opt-in configuration are not runtime evidence.

Next implement exact failed result/receipt/event/outbox custody and terminal step/operation guards, including denial of automatic requeue after a captured failure. Generic pre-dispatch infrastructure errors need their own bounded semantics; never invent a provider capture or candidate for them. Then configure the executor/worker, mutation transports and authenticated reads, committed-publication recovery, actual process-kill windows and supplier reconciliation. Default/public extraction admission remains closed. Array evidence, claims/report/adjudication, case/score completeness, disconnected export/replay, security and deployment, Temporal/Cursor/EVE cutovers, dashboard/human review/calibration and full46-row acceptance audit remain open.

Evidence in parent `internal/`:

- `verification-structured-extraction-failure-1777342b-c995-4d96-9873-73afdf3409aa.json`: SHA256 `aa6bc20fd4ed5034fde2b4545ed70e23b2d0a78e5d41a67d01883a9fac1f32fa`
- `verification-structured-extraction-failure-audit-20260906.json`: SHA256 `1dbea7bc35bad48b0be3babff413e9ec5866b4781c30701ed5f482d1084facd2`
- `verification-structured-extraction-failure-workspace-20260906.log`: SHA256 `ea22ddf76208bc498e2d1e703e056163786d23d85e9662091bcd751e2e350b97`
- `verification-contract-0225-audit-20260906.json`: SHA256 `cdf47af967a0884f038bb1b41722f9f1bafc157427f819cbc029b607a3003ade`
