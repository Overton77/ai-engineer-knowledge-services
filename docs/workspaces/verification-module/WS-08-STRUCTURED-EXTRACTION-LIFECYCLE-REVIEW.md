# Canonical structured extraction candidate checkpoint (EV-081)

Accepted bounded lifecycle checkpoint on 2026-09-06 UTC. The complete verification-module goal remains active.

`PostgresStructuredExtractionLifecycleStore` initializes a canonical operation/step/producer-attempt identity under the actual live lease. It binds the separate operation-request and step-input digests, authenticated operation context, full source/profile/schema/projection/transformation identity and prompt/schema digests. The actual service envelopes have different request and step hashes; the proof uses PostgresKnowledgeOperationService rather than a fabricated equal-hash input. Structured extraction is now verification-owned for context/attempt storage, while default admission stays closed.

The lifecycle transitions running -> retaining -> retained. Database timestamps preserve original start, retention start and completion across exact retries and higher-fence replacement. The candidate builder uses canonical retentionStartedAt. Capture retention works after the response is committed while the provider accounting response reference is still NULL; the original capture/dispatch fence remains authoritative. Dispatched calls are never retried automatically. Provider failure publication is not implemented by this checkpoint.

Root review strengthened the subagent draft with frozen caller snapshots before awaits, millisecond comparison against microsecond capture timestamps, canonical candidate-envelope and provenance payload hashing/byte lengths, and projection checks against stored artifact hashes. SQL independently enforces canonical operation/step ownership, native artifact/source ancestry, exact registered candidate/precontext/provenance types/parents/semantic signatures/producer attempt/timing, immutable identity and monotonic state transitions. No unbound outputDigest column is stored; that value belongs to authenticated candidate/provenance payloads. The checkpoint is an unverified candidate and has no verification seal or source-authority claim.

Local migration317 is applied and database-contract0.2.20 matches180 canonical shipped files. Types generation/check and database typecheck pass. An audit caught a syntax-only composite-field edit made after migration application. Root restored the source to the actual applied SQL, then reran the proof. Final evidence below matches both applied function bodies exactly; the earlier db3aba proof/draft hashes are historical. No migration history was rewritten in the database and no remote rollout occurred.

Final actual local proof passes13 named checks using both Gateway and Interfaze adapters with injected synthetic transport. It covers default denial, two canonical input digests, original capture before accounting settlement, live-claim direct SQL artifact/identity substitution rejection, exact retention/timing, natural lease expiry and higher-fence replacement without redispatch, cancellation and the still-closed terminal success guard. Final proof makes two synthetic original requests and no supplier calls. Both provider rows settle synthetic10-micro costs at ordinal0. Prior synthetic proof records remain separately retained; this is not a total-budget accounting claim.

Independent audit downloads19 actual Storage objects and verifies native payload hashes, ordered ancestry, semantic signatures, exact precontext bytes against raw response, both candidate chains, canonical lifecycle/capture/timestamps, settled provider rows, same/foreign-tenant app_reader isolation and two applied SQL function bodies. All proof operations are cancelled after checkpoint checks. This is not successful terminal publication, public extraction routing, a process-kill proof, supplier billing or provider-quality evidence.

Six focused persistence lifecycle tests, standalone proof typecheck, persistence typecheck/build and all72 uncached workspace typecheck/test/build tasks pass (2m5.36s). Skipped opt-in tests are not used as runtime evidence.

Evidence in parent `internal/`:

- `verification-structured-extraction-lifecycle-4ef9e506-d4ca-44ce-9387-488c7f4ae24e.json`: SHA256 `d9a8b825232b700cf820c5717948cec28a1a07071c9d520725887b99c12db62a`
- `verification-structured-extraction-lifecycle-audit-20260906.json`: SHA256 `2357c02a69240918188fecb531738dd0d3ca5b32f9ef0e51680c5bc870254ad8`
- `verification-structured-extraction-lifecycle-workspace-20260906.log`: SHA256 `f697ba0278a6f4a6842516bfa99b02832defb638b30571f92685904422e92384`
- `verification-contract-0220-audit-20260906.json`: SHA256 `3b085d35bce3045631401ed4835d9ee502b1e5b1ce709f75700eb9d76812f5b1`

Next: final immutable result publication, exact step/operation success receipt guards, and durable provider-failure outcomes. Extend initial identity with immutable runtime/code provenance before live execution; current checkpoint proofs do not contain a complete run manifest. Keep migration315's terminal denial until its replacement checks are reviewed. Then configure the actual executor/worker, HTTP/client/CLI/MCP/authenticated reads, actual process-crash windows and supplier reconciliation. Array evidence, claims/report/adjudication, case/score completeness, deployment/runtime cutovers, dashboard/human labels/calibration and full acceptance audit remain active. No whole acceptance row is closed by this slice.
