# Structured extraction execution and publication custody (EV-082)

Accepted bounded execution/publication custody on 2026-09-06 UTC. The complete verification-module goal remains active.

Execution provenance is now an immutable canonical binding established before any operation-scoped provider reservation. It binds tenant, operation, step, producer attempt, separate request/step hashes, profile, runtime/code identity, execution mode and original creation time. The SQL guard and provider accounting serialize on the same operation row. Replacement leases can reuse the original binding; they cannot introduce a later code identity after reservation.

Dirty execution requires a dedicated `verification_structured_extraction_source_custody` artifact. Its strict canonical payload records tenant, `listed_files` scope, safe logical KS/DB paths, exact base64 bytes, per-file byte lengths and SHA256 digests, and a bounded total. Execution preparation and final publication freshly authorize and hydrate these bytes and validate every file. SQL independently requires the dedicated artifact type. This proof retains twelve actual source files, explicitly not a complete repository or dependency snapshot. The workspace has no committed repository identity, so runtime code is `uncommitted` and dirty. The local Windows worker has no container image claim; the native parser image remains separate source-preparation evidence.

The final publication builder freshly hydrates execution, source custody, producer profile, raw cost evidence, native candidate/provenance and optional precontext. It validates original capture/fence, full ancestry, canonical bytes and original lifecycle timestamps. Its Ed25519 signature attests only to artifact custody. Output remains `unverified_candidate`, with `shape_only` schema validation; this does not establish source authority or semantic accuracy.

`PostgresStructuredExtractionPublicationStore` verifies the signed canonical body and hashes before committing under a live lease. The append-only publication row binds the exact retained lifecycle, execution and provider-accounting snapshot. SQL reconstructs the provider call hash from canonical attempt/budget/model/ordinal/reservation/state/actual-cost/raw-evidence fields and reconstructs the ordered-parent publication signature. Provider configuration is bound through the hydrated registered profile and signed manifest, not decoded independently by SQL. Unknown cost is null. Pricing basis explicitly distinguishes synthetic transport from a provider-reported response; supplier billing is not verified. A later accounting change invalidates a not-yet-committed stale publication.

Root review corrected ordinal zero admission, added final dirty/precontext hydration and strict source semantics, and removed the parser-image-as-worker-image claim. Applied migrations318/319 were preserved; follow-up320 adds typed source admission. A post-proof contract edit was detected by the native current-source audit, so proof855b was discarded as acceptance evidence. Intermediate7ea28 and d4a520 runs and database-contract0.2.21 are superseded by the final evidence below. No applied migration was rewritten and no remote migration was run.

The final actual local proof passes25 named checks through Gateway and Interfaze with injected synthetic responses. It observes real PostgreSQL lock waits for both race outcomes: execution binding wins before reservation, or reservation wins and the competing binding is rejected. It also tests wrong source artifact type in SQL, actual dispatched-to-settled accounting drift after signing, forged/untrusted publication rejection, artifact/identity substitution, immutability, natural lease expiry with higher-fence replacement, exact CAS reuse without redispatch, cancellation and terminal denial. Three original synthetic adapter calls are made: two produce committed custody publications; the third proves late-binding denial. All three settle synthetic10-micro costs at ordinal0. No supplier request or total historical-budget claim is made. All proof operations are cancelled after the checks.

The independent audit downloads27 actual Storage objects and validates native hashes/canonical bytes, source-file bytes against current files, exact precontext, Ed25519 signatures including tamper rejection, ordered parents, canonical execution/lifecycle/accounting/publication rows, rejected stale publications, wrong-type source nonadmission, same/foreign-tenant app_reader isolation, and five actual SQL function bodies against migration source. Provider `created_at` uses transaction-start `now()` and is not a reservation-commit timestamp; concurrency evidence uses observed row locks, and timing checks use the actual `dispatched_at` clock timestamp.

Four focused contract and nine focused application tests pass. Final full packages contain38 contract and184 application tests. Standalone proof typecheck, local database type generation/typecheck, and all72 uncached workspace typecheck/test/build tasks pass (2m9.65s). Database-contract0.2.22 matches183 shipped canonical files including migrations318-320. Skipped opt-in tests are not runtime evidence.

Evidence in parent `internal/`:

- `verification-structured-extraction-publication-3d7598ec-cd74-4af3-b870-23859fc706d1.json`: SHA256 `e11c9befc91db9ea0434a5f989af34a735c04dfa91685fdd3b912d0d6cef85a0`
- `verification-structured-extraction-publication-audit-20260906.json`: SHA256 `a129c3930a1622885537b09d437e430a7b26aa581d6e79964a0fa498886b1461`
- `verification-structured-extraction-publication-workspace-20260906.log`: SHA256 `fe78a2318bc2dfad77afc6c110369b1652cd2d5eca41c81dd1dcea11fb1dbbdd`
- `verification-contract-0222-audit-20260906.json`: SHA256 `a8539a9ee8cd6c059af376c41df60351b6e3bde5d36b44f3674d1235d5a59edd`

Next: exact structured-extraction step/operation success receipt guards and durable no-candidate failure outcomes; then configured executor/worker, HTTP/client/CLI/MCP and authenticated reads, actual process-kill windows and supplier reconciliation. Migration315 still denies terminal success, and default extraction admission remains closed. This checkpoint does not complete `extractStructuredData`, public execution, live supplier billing, semantic verification, full disconnected replay, array evidence mapping or any whole acceptance row. Claims/report/adjudication, canonical case/score completeness, security/deployment/runtime cutovers, dashboard/human labels/calibration and the full46-row acceptance audit remain active.
