# Independent persistence review

Coordinator accepted the bounded WS-03 deliverable on 2026-09-05 after code/migration review, independent 25-check DB/replay and 9-check HTTP Storage proofs, affected checks, full-verification log audit, and byte-for-byte package-pin audit. EV-012/EV-013 contain the evidence. The table records implemented fixes; acceptance does not extend to the composition and rollout gates below.

| ID | Finding | Required proof | State |
| --- | --- | --- | --- |
| PR-01 | Supabase storage collision repair can overwrite using `x-upsert: true`. | Verification defaults to verify-only; conflicting/missing-metadata objects cannot be overwritten. | Implemented; EV-012 collision bytes unchanged, runtime regression 11/11 |
| PR-02 | Legacy tables receive required columns without a migration path for nonempty deployments. | Explicit compatibility/backfill or safe preflight and remediation; no silent empty-table assumption. | Implemented; revised first migration keeps legacy additions nullable and runs ownership preflight before composite constraints; additive follow-ups reconcile local applied history; fresh-chain deployment verification remains required |
| PR-03 | Same-tenant representation artifact FK alone does not bind a locator to the capture's provenance. | Unrelated artifact rejection and valid derived-projection success. | Implemented; EV-012 valid projection and unrelated-artifact rejection |
| PR-04 | `eval_score` tenant policy was not in the refreshed RLS list. | Inspect grants/policies on all affected tables and prove cross-tenant read/write denial. | Implemented; EV-012 actual row own-read/foreign-hide |
| PR-05 | CAS path format did not bind filename and folder prefix to the row digest. | Negative direct SQL test for wrong digest filename and prefix. | Implemented; EV-012 wrong-path rejection |
| PR-06 | Terminal insert bypasses the lifecycle update guard's timestamp ordering check. | Reject terminal rows ending before start on both insert and update. | Implemented; EV-012 terminal-time rejection |
| PR-07 | Audit handle identity collision checks compare only digest/length; hydration compares a partial metadata subset. | Reject rewritten immutable lineage/transformation/classification metadata. | Implemented; full canonical handles compared and regression passes |
| PR-08 | Parent IDs and explicit lineage edges have inconsistent cycle checks. | Reject cycles and inconsistent lineage in both representations. | Implemented; two-way consistency/cycle regression passes |
| PR-09 | Inspection omits policy and tenant binding checks performed during sealing, and outer bundle schema is TypeScript-only. | Strict runtime parsing; policy/tenant tamper cases fail even with recomputed unsigned digest. | Implemented; strict outer/seal keys, tenant/policy/manifest checks |
| PR-10 | Signature validation may throw outside the inspection error boundary. | Malformed signature and verifier failure produce an invalid inspection result. | Implemented; both resolve to invalid inspection |
| PR-11 | Public-field deny regex misses camelCase secret/reasoning keys and is not a sanitization boundary. | Explicit public contract and robust forbidden-field tests; do not claim arbitrary values are secret-free. | Implemented; normalized credential/private-reasoning guard with explicit token-usage allowlist |
| PR-12 | PostgreSQL CHECK accepts NULL; the compatibility migration's compound v1 locator/run checks do not explicitly reject null state/count/status. | Direct SQL NULL cases rejected for every required v1 field. | Implemented; EV-012 direct locator/run NULL rejection |
| PR-13 | Compatibility lifecycle guard compares only legacy columns, allowing new identity columns to change during legacy finalization. | Full row comparison excluding only the permitted terminal field. | Implemented; EV-012 forged legacy finalization rejected |
| PR-14 | Capture ID collision check compares only artifact/hash, ignoring source/time/method/context changes. | All immutable persisted capture fields match on idempotent retry. | Implemented; full immutable comparison and negative proof |
| PR-15 | Judgment insertion hardcodes `replay_signature_match=true`, including semantic/human judgments without replay proof. | Replay status derives from an actual verified receipt or remains explicitly unknown/not performed. | Implemented; trusted composition supplies optional replay status, model judgment defaults to NULL |
| PR-16 | Audit load validates a bundle but does not yet compare inner identities against the requested run's canonical row. | Reject mismatched tenant/run/policy/result/bundle identities even when the stored bundle is otherwise valid. | Implemented; canonical row/handle/digest bindings and mismatched requested-run proof |
| PR-17 | Authorization tickets keyed only by tenant/artifact may cross requests in a shared repository instance. | Principal/request-scoped resolver lifecycle or unforgeable bounded authorization capability; no singleton ticket reuse. | Implemented; one pending, consumed ticket per resolver instance |

PR-02 follow-up: a later migration dropping NOT NULL cannot make the earlier ADD NOT NULL succeed on populated legacy tables. Prove the migration path before the first migration or record an explicit preflight/remediation process. Do not infer compatibility from the final schema alone.

Changes to already-applied SQL should be additive follow-up migrations. Existing user data and the preservation checkpoint must remain intact. Completed proof logs must use unique names before hashes are recorded.

Composition gates: credential-key filtering is a guardrail, not proof that arbitrary string values are secret-free; WS-06/09 must construct allowlisted public provider/configuration projections. Optional replay status must be derived by trusted runtime composition from an actual replay, never copied from a caller assertion. Production principal resolution, symbolic-ID mapping, retention lifecycle and fresh-chain deployment validation remain later work. The initial nullable migration revision plus additive local reconciliation are documented in the handoff; no production deployment is claimed.
