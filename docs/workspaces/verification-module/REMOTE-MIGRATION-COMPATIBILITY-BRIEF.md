# Shared-bucket migration compatibility brief

Bounded follow-up owner task after WS-07: resolve B-002's populated-data blocker, then execute a reviewed remote rollout and independent verification. Read the full Supabase skill, canonical README, remote preparation and prior persistence handoff first. Coordinate database/type changes with active benchmark calls so a deployed constraint cannot invalidate the older running repository build.

The target remote project has 236 legacy artifacts in `ai-engineer-cloud-bucket`, all outside the new verification CAS path conjunction. Global constraints in these migration files would reject existing records and legacy writers:

- `20260905010000_verification_persistence_contract.sql`
- `20260905011000_artifact_registration_lifecycle.sql`
- `20260905013000_verification_legacy_compatibility_and_isolation.sql`

Coordinator verified all three files are untracked, newly authored in this mission, and not committed canonical history. They have already been applied to the disposable/local development database and included in the preserved 0.2.6 vendor archive. Any correction must preserve that archive and historical receipts, explicitly document revised unpublished SQL, and add a convergence migration for the already-migrated local database. Do not rewrite or repair the remote historical ledger.

One proposed solution to review is an explicit immutable verification-contract marker on the shared artifact row. Legacy rows/writers retain a null marker and their true bucket/path/metadata. Verification repository registration writes `verification.v1` and the strict CAS/media/size constraints apply to marked artifacts; verification metadata insertion must require the corresponding same-tenant artifact marker, preventing an unmarked artifact from being silently adopted as verified. New verification rows cannot downgrade the marker. Backfill the marker only for existing validated verification metadata, retaining all original handles and content hashes. This is a proposal, not an accepted implementation; verify all bypass and collision paths.

Required proof: an isolated pre-service canonical database populated with representative non-CAS legacy artifacts must apply the complete corrected chain without changing those identities; legacy-compatible writes still work; malformed new verification paths/metadata/marker downgrades fail; valid new CAS artifacts pass real Storage/replay. Existing 89-chain empty proofs do not cover this requirement. Preserve and extend the real provider accounting and artifact governance proofs, regenerate canonical types, publish a new local vendor version and verify byte parity.

Remote schema metadata already matches 5,702 shared baseline objects, with only rolling observability partitions differing. Aggregate ownership and backfill checks are in `REMOTE-DEPLOYMENT-PREPARATION.md`. Review remaining populated checks before rollout. Use the original canonical pending SQL and record its hashes. A separate deployment workspace may mirror the actual remote migration ledger solely to select missing migrations; it must never execute a placeholder or historical teardown and must first prove the exact dry-run list. CLI help for `db push --db-url --dry-run` and `migration up --db-url` has been checked. Do not expose credentials in command output or saved receipts.

The user already authorized necessary migrations. No extra permission is introduced by this brief. Database rollout is still separate from production service/worker readiness and human/provider policy promotion.

## Independent populated rehearsal — 2026-09-05

The coordinator reproduced B-002 on a separate synthetic database. Starting from the 50-migration baseline, the harness inserted 236 representative legacy shared-bucket artifact rows with non-CAS paths and nullable historical media/size fields, then applied the actual pending canonical migrations incrementally. The chain stopped at `verification_bucket_cas_path_ck` with SQLSTATE 23514; the ledger reached 67 migrations through `20260904015000`. All 236 original artifact identities and selected metadata remained byte-for-byte unchanged. No shared local or remote database was written.

Receipt: `internal/verification-populated-chain-audit-populated-4052de36.json`, SHA-256 `7c2ded64addfc14c6de2ffde60939e4398d3ba65bc73dd4f7bbb9a69a4a5dc3b`. Harness: `internal/verification-populated-chain-audit.mjs`; isolated workspace `internal/fresh-chain-populated-4052de36`, exact container `supabase_db_vfy-populated-4052de36` stopped and volume retained. This is an expected-failure reproduction, not migration acceptance. The next owner should rerun the harness against corrected SQL in a new disposable workspace and require complete success plus unchanged legacy identities.

Additional source-review notes for the implementation owner: `PostgresVerificationRepository.registerArtifact` inserts the base artifact and metadata in one transaction with `ON CONFLICT DO NOTHING`; its collision check must include the immutable contract marker so an existing unmarked legacy row cannot be adopted accidentally. The metadata trigger must independently reject unmarked or cross-tenant base rows. The current generic artifact immutability trigger compares all JSON fields, so a convergence backfill will otherwise reject the new marker update. Use a narrowly scoped migration transaction that validates the exact metadata-backed rows and preserves all other columns; do not leave a runtime marker-upgrade or downgrade path. Audit the parent/attestation availability query and full replay closure rather than treating an available legacy parent as automatically verified. Coordinate the repository build and incremental convergence migration only after the active benchmark finishes.


## Corrected populated rehearsal — 2026-09-05 09:13 UTC

The coordinator preserved all three original unpublished SQL files under `internal/verification-artifact-marker-originals-bde38942-cecd-4a6f-9e6e-85a6208ca113/` and revised their CAS checks to depend on an immutable `verification_contract_version` marker. New convergence migration `20260906020000_verification_artifact_contract_marker.sql` validates and marks only existing metadata-backed CAS artifacts inside an exclusive-lock transaction, then enforces marked same-tenant metadata admission and registered available parent/attestation closure. Runtime marker changes remain forbidden. Repository wiring is under review; no shared or remote database has received this migration yet.

The isolated full 91-migration chain passed with 236 synthetic legacy identities unchanged. Legacy writes and valid marked root/child metadata passed; seven malformed marker/admission cases failed as expected. Receipt `internal/verification-populated-chain-audit-populated-b79bad14.json`, SHA-256 `684b300ad4ab05e546bd9802a9c841ea536c2786f2025d9bd291bbbff6fa60af`. Proof SQL SHA-256 `322ed93cc3975dd95799779e39baf88844d28f0dcc8eb62071db32facbe7cfc3`. Isolated container `supabase_db_vfy-populated-b79bad14` is stopped with volume retained. Independent SQL/repository review, populated local convergence, new canonical type vendor, real CAS/replay regression and remote exact-ledger rollout remain outstanding.


### Independent review follow-up and stronger marker proof

The admission agent independently accepted the repository marker wiring but found missing downstream consumer guards and incomplete historical parent/edge validation. Root hardened `20260906020000` with bounded lock/statement timeouts, parent signature/distinctness/self checks, exact generated edge matching, incompatible-edge rejection, and a recursive parent/attestation DAG check before marking. Future metadata requires complete generated edges at transaction commit; lineage inserts must match immutable parent metadata. Current SQL SHA-256 `3129d4146ae1aff050462c4f1ed3c58a41939a910251dbd2acc5ddbc089df7c0`.

The stronger isolated 91-chain proof passed all seven backfill probes (one positive, six adversarial), nine runtime admission negatives and valid root/child/legacy writes while preserving 236 original identities. Receipt `internal/verification-populated-chain-audit-populated-5ac3b9f8.json`, SHA-256 `763ba33fd3587091c45e4c3e951600bf325f43126ed786b75884685fe19930d1`. Its container is stopped and retained. The original shared local 886 artifact rows and 886 metadata rows are preserved privately in `internal/verification-marker-convergence-snapshot-4c3c8fe6-8be1-4e1d-b63e-27adbe3d5020.json` (SHA-256 `1f19f383fea72c914a8fc3d3f2d05664112e6622d17ba1d3481abc4b4b25c879`). At this checkpoint no shared migration had been applied. Admission agent owns downstream migration `20260906021000`, EV-036; coordinator reserves EV-037 for independent acceptance. Both markers and downstream gates require final review before rollout.

## Local convergence and independent consumer acceptance — 2026-09-06

The final canonical pair is marker/backfill migration `20260906020000` at SHA-256 `3129d4146ae1aff050462c4f1ed3c58a41939a910251dbd2acc5ddbc089df7c0` and consumer-admission migration `20260906021000` at SHA-256 `3e8abdf25c3c82eb0e4132aafdde137e5ca25f631f4a6fcc851b4c0376e903ea`. The independent fresh populated 92-chain preserved all 236 legacy identities and passed seven backfill probes, nine runtime admission cases, and all 39 consumer cases. Receipt `internal/verification-populated-chain-audit-populated-fb51c87a.json`, SHA-256 `a87a64b2811afdc5ffd2825fbc50f202d0d8be2150398c3aa35f39225bbb7d29`; the isolated container was stopped and retained.

The coordinator then applied both migrations incrementally to the shared local database without reset. The before snapshot `internal/verification-marker-convergence-snapshot-4c3c8fe6-8be1-4e1d-b63e-27adbe3d5020.json`, SHA-256 `1f19f383fea72c914a8fc3d3f2d05664112e6622d17ba1d3481abc4b4b25c879`, and after snapshot `internal/verification-marker-convergence-snapshot-e96c4714-ba23-4b7e-96cf-5e36bb9fc7d7.json`, SHA-256 `a351ac1a9e9dc3452e45646afaafbe93bee5da6283dfbc3d3fed3ad223972fb0`, contain the same 886 artifact identities and 886 immutable metadata rows; only the validated verification marker changed, and the local ledger advanced from 90 to 92.

Canonical vendor 0.2.8 parity passes for the 92 migrations and generated types (`internal/verification-vendor-parity-review-f50aec1b-da78-4a9a-a854-7921fea55533.json`, SHA-256 `93409c805573820038b6853f6edc69668c76338ef269820c75a04c404625e4f6`). The real Postgres/private Storage persistence proof passes 26/26 (`ai-engineer-knowledge-services/catalog/verification-proofs/verification-persistence-d95c61c9-7977-441c-80b9-ca9c691c03d6.json`, SHA-256 `e0d5bc4d50bc40fe19750cea79d06e779dcd18ccb685a4fcd8a364745d695444`), durable synthetic provider accounting passes with physical Storage and zero dispatches (`internal/verification-provider-accounting-proof-2aa68300-5ada-47f4-92e2-c2e55cbe0ae6.json`, SHA-256 `3393e004493f01ccdd7be6fbc336294f7425819eeca829d0a63c406c888d55d4`), and read-only post-migration custody resolves all 709 benchmark artifacts and 117 attempts (`internal/verification-extraction-custody-review-1bc5b682-7f24-45d4-a69e-ea12ee796713.json`, SHA-256 `0a0a000c0007b578407b3ea133153a11a632cf812171286c971036866e25807b`).

B-002 is narrowed to remote deployment. No remote database was written. Before rollout, the coordinator must prove the exact missing-migration list against the remote ledger, execute the reviewed compatibility path without historical-ledger repair, and independently verify remote identities, marker convergence, consumer guards, Storage replay, and service compatibility.

## Remote partial rollout and identity-scope correction — 2026-09-05

The reviewed first dry run selected 42 canonical migrations against 116 historical ledger entries. TLS uses the official Supabase production CA and verify-full; execution imposed lock_timeout=10s and statement_timeout=120s. The first 17 migrations committed through 20260904015000. Migration 20260905010000 rolled back on a previously uncovered global digest/type uniqueness constraint: the existing 236 artifacts include 23 legacy workspace_file duplicate groups with 50 excess records. No records were deduplicated or deleted. Failure receipt `internal/verification-remote-rollout-result-c3238e2d-e8cb-44a1-b5e5-ccf63fb46328.json`, SHA-256 `6a8fe5843b4d133bd75ccc7fd36794cf146d5de261f17d4fd537cb90a47f180b`.

A read-only comparison of all 295 original application tables found every original-column row digest unchanged after the partial rollout; artifact/capture/claim counts remain 236/31/35. Receipt `internal/verification-remote-preservation-e48ba467-8712-4886-a042-a59f692b9d83.json`, SHA-256 `bf3f1bcbdfc791abde8d779b796eb8b9ddde9bc8da5531eebda9f3eca1562606`.

The original failed SQL is preserved in `internal/verification-artifact-uniqueness-originals-c71383a1-fe21-43d6-927a-bf1e2439d506/`. Its correction scopes tenant/digest/type uniqueness to immutable verification.v1 markers; legacy custody IDs remain distinct. Corrected 050100 SHA-256 `deb83b7828ff3534fdba3a77fabba61774b4845c6e02bbfbde8003d9409e67a6`; new local-convergence 060220 SHA-256 `264185306a8e8552a56abc2c317f0312a305f0668ba686c9af5b67f57d893126`. Both received independent source review. The 93-chain rehearsal now contains 118 duplicate legacy digest pairs, preserves all 236 identities and passes seven backfill, nine runtime and 39 consumer tests: `internal/verification-populated-chain-audit-populated-93526b5e.json`, SHA-256 `6e6d0fb3bb417c10f3d07e92dad95396dc08d86407012f80d77a1adf70b96602`. Separate index-predicate, coexistence and duplicate-rejection proof `internal/verification-isolated-sql-proof-ee73e99a-48d3-4b65-b6fb-6915d0a52ab5.json`, SHA-256 `b2f8eb0aae0c395552ce6eaefd842c2c1c136bb83806e404fb0dc0e8d7f111a2`, also passes.

Shared local convergence is now 93 migrations. Canonical vendor0.2.9 contains byte-identical 93 SQL files and five TypeScript sources; parity receipt `internal/verification-vendor-parity-review-872f59bb-294d-420e-8f98-465b4b277c50.json`, SHA-256 `f703a0519b6bd578d1b7c66c27ceea3122faf193accff4dde071e3408e0a9e52`. Generated types are current. The old remote plan and apply helper are intentionally stale and preserved. New plan `internal/verification-remote-rollout-7088257a-841e-4bcc-98e1-8703fbf19fa0/plan.json`, SHA-256 `4be166edcb22dc8d036ebd025b664ea712b90fe4c45d6db766f668aca93bc082`, selects exactly the remaining 26 migrations against the actual 133-entry ledger. Remote continuation and post-rollout acceptance are still pending at this entry.

## Final rollout acceptance — 2026-09-05

The reviewed resume completed all 26 remaining migrations, yielding the exact 159-entry remote ledger through `20260906022000`. EV-040 independently compares the remote schema/security with a fresh canonical 93-chain; only expected dated observability partitions differ after ACL and line-ending normalization. EV-041 records coordinator acceptance, original-data preservation, and real remote persistence/accounting proof receipts. The prior pending-plan and partial-apply sections above are historical evidence, not the current rollout state. No further convergence migration is warranted for the shared local database's redundant marker-check difference. Application runtime deployment and cross-surface service execution remain separate unproved gates.
