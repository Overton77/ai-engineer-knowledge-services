# WS-07 benchmark implementation brief

Root preparation for the next sequential owner, after reviewed WS-06. Read specification 15, especially mandatory diagnostics pack 15.9, acceptance matrix, decisions D011/D012 and completed component handoffs. Own the actual benchmark runner, dataset contracts/tooling, report generation and independent review of coordinator-authored statistics/preparation. Do not infer model quality, gold labels, approved redistribution or production readiness from component tests.

## Frozen local evidence already prepared

`internal/verification-offline-source-preparation-9093b465-16c2-4cc1-b7f4-728d3958b0ed/` contains 82 registered artifact byte files, complete ordered parent closure, original registry wrapper and canonical source/capture metadata. Export manifest SHA-256 `50a3552cffc639a78d7789865be929baefd64d2342431e2de958e669c21f163e`. Root checked full handle/byte identity and re-admitted all 17 projections for 16 sources using the application envelope path. No parser invocation or reacquisition occurred. WS-06 independently reviews parser v2 before benchmark admission.

`fragment-candidates.json` SHA-256 `d1781f1c5b32dff5fff3a27251c9072386f1673e98d4fb9210090fc4ef6b9831` contains 1,186 HTML candidates. Each has an exact DOM selector, selected text digest, capture, full projection identity and transformation-envelope ID. Every selector was replayed against registered bytes. Keyword/structural selection is explicitly a candidate index, not complete claim extraction. Parent DOM paths allow wider context inspection; short text leaves must never be detached from sample/population/heading qualifications. Repeated layout copies remain distinct locations, not independent sources. PDF text/geometry projections are also present in the artifact inventory.

`pilot-case-proposal.json` SHA-256 `ae9bce06f634715b7a6f8ff278839347433e7f4889cac6f9b25fa1bcd49823c8` offers 40 cases: 20 literal source-statement seeds paired with deliberate mutations. These are coordinator proposals and explicitly ineligible for model-quality scoring until label/scope review. They are not necessarily atomic propositions; run and review actual semantic decomposition, preserving qualifiers and offsets. Cover remaining PDF graph abstention, corrupted selectors, biomarker aliases/lists/headings and entity-binding cases within the 30–50 case Pilot. Keep originals and mutations in the same split/cluster and also preserve source-family grouping. Never describe the 40 proposals as 40 independent observations.

Preparation scripts are `internal/verification-export-frozen-sources.mjs`, `verification-index-frozen-fragments.mjs`, and `verification-propose-diagnostics-pilot.py`. They refuse overwrite and verify upstream hashes. Final dataset creation must register its new manifests/cases/labels in canonical evaluation persistence and include the complete approved restricted offline replay package, rather than merely publishing these preparation files.

## Required experiment and reporting

Compare baseline, Interfaze, cascade and consensus/abstention on identical immutable cases with fixed configuration, judge identity, seeds and budget. Reuse exact cached calls only with explicit per-arm attribution and disclose shared calls. Persist case checkpoints and every dispatch attempt, including unknown usage and provider failure. Do not make extra root/provider calls outside the accepted successor budget. After the local reset incident, only budget 5890d523-561b-55db-a724-890d642885d9 (key ws07-pilot-recovery-20260905, ceiling 17,928,167 microUSD) is spendable; 2,071,833 microUSD of historical liabilities remain separately carried. The original USD20 budget pointer is historical and must not be recreated. Read LOCAL-RESET-RECOVERY.md and the sealed V4 extraction experiment. Begin with WS-06 actual conformance receipts, then freeze the Pilot before running its full matrix. Conformance tuning cases must not masquerade as held-out tests.

Schema validity, field mechanics, citation precision/completeness, semantic support, source authority, world correctness, attribution diagnostics and calibrated confidence remain separate. Human labels must be genuinely human; implement annotation/adjudication tooling and pending state if no authorized human labels exist. Agent-reviewed engineering expectations can drive deterministic regression demonstrations, but report their provenance and avoid estimating clinical or human-gold model quality from them. Unknown licensing is not approved by this brief.

Independently review `HANDOFF-WS-07-STATISTICS.md` and the actual statistics code before using it. Root's separate Python Decimal/exact-integer reference script verifies 743 numerical cases; it is additional evidence, not independent review of the root-authored estimator. Include paired cluster bootstrap with small-cluster limitations, exact McNemar, Wilson denominators, multiple-testing handling, calibration only where valid labels exist, abstention/risk coverage, failure slices, latency/cost/unknown usage and catastrophic errors. Do not manufacture probabilities from raw provider confidence.

Generate both company HTML/Markdown/JSON reports, comparison and audit with machine claim/field/source/run ledgers and a sealed bundle. Every material factual sentence requires a capture-bound evidence path. Preserve Tru 2–4 versus 3–4 weeks after lab receipt, GL 19 versus 21 wording including same-page footer conflict, triplicate consistency versus population accuracy, and publication versus commercial-product scope. Reports should distinguish company claims, paper results, reviewer inference and unresolved questions; preserve informational/non-diagnostic qualifications without patient-specific advice.

The installed offline demo is owned jointly with WS-08; provide a reusable deterministic application/harness entry point and frozen artifacts for its public CLI. Refresh must propose a new version, emit a drift report and leave old bytes/results untouched. No full copyrighted pages or reports should appear in public output. The internal full-capture preparation retains restricted custody and is not authorized for wholesale provider upload.

## Benchmark v1 and annotation boundaries

The specification also requires 150–300 real, stratified Benchmark v1 cases after the Pilot; a 40-case demonstration alone does not complete WS-07. Build the versioned case import, source-family split checks, annotation/adjudication workflow, sealed experiment runner and applicable metric adapters so Benchmark v1 is executable. Prepare real source-bound candidate cases without padding the count with duplicated layouts or treating generated mutations as independent observations. Preserve separate development/calibration/locked-test states, and never tune thresholds against locked labels.

Dual independent annotation and expert adjudication for high-risk/locked cases are specification requirements. Agent engineering review is not a substitute for a human annotation event. Missing human reviews should leave explicit pending records and prevent promotion/calibration claims; they should not stop implementation of the runner, reports, service integration or review UI. Record which fields require human action with exact case IDs and a concrete review artifact. Production shadow results likewise require real future research/ingestion traffic and cannot be fabricated from the offline demo.

