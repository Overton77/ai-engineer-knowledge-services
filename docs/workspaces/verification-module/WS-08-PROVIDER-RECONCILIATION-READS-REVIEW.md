# Historical reconciliation reads

EV-095, coordinator review, 2026-09-06. PostgresProviderReconciliationReadRepository now authenticates an already applied original-attempt decision after its admission window has expired. It requires the terminal extraction operation, immutable reconciliation ledger and matching settled provider accounting. It loads registered Storage evidence, authenticates the operator signature and native execution/request/billing bindings, and rechecks the ledger snapshot after hydration. Its output is the compact application result with redispatchAuthorized:false.

ProviderReconciliationAdmission.verifyEvidenceAt validates evidence at the durable appliedAt time using a separate issuer. It rejects future application times, retains current configured operator/key authority checks, and returns no usable mutation permit. The existing admission instance still rejects expired decisions and rejects historical results at assertPermit/apply. Historical verification alone does not establish that a decision was applied; only the repository's native ledger checks provide that fact. Public callers must be authorized before invoking this internal control-plane repository.

The local proof reads the four EV-094 settled decisions using a clock one day after each expiry. Actual local ledger rows, actual Storage bytes and Ed25519 signatures are verified. Every compact result equals its original application result. Sixteen grouped checks cover expired reads versus mutation denial, cross-tenant/missing operation denial before Storage, unknown signing keys, corrupted billing bytes, unchanged native accounting/ledger, duplicate rows, incorrect actual costs and snapshot drift. Query-result faults are injected over actual database reads; they are not concurrent database mutations. No supplier requests or accounting mutations occur.

Final proof: parent internal/verification-provider-reconciliation-reads-272dc9dc-775c-433f-952b-f14c4fef10c8.json; SHA256 6255d6d895794afa71907e6bf953d5cab65d6c156193ffee695a83760c2c17e2. Three implementation/proof source hashes are retained in the report. Dedicated proof TypeScript passes. Reproduce with local helper provider-reconciliation-reads and VERIFICATION_EXTRACTION_WORKER_RECEIPT set to the EV-094 publications proof filename. Do not load the remote KS .env.

No migration or database-contract change in this slice: local330/0.2.29 remain current; remote unchanged. Actor-authorized operator runtime, HTTP/client/CLI/MCP mutation/read transports and live supplier billing remain open. Partial-retention/cancellation windows, semantic custody, case/score completeness, replay/export, security/deployment, runtime cutover, dashboard, human review and the final acceptance audit remain open. No full acceptance row is closed.

EV-095 workspace validation: all72 typecheck/test/build tasks passed uncached in2m8.561s. Log parent internal/verification-provider-reconciliation-reads-workspace-20260906.log; SHA256 ccfa21e7eef9eb8a78caa7b499f603ec283482428f1220b27c4a3067ea9fcde9.
