# SW-12 acceptance completion map — 2026-09-08

## Boundary and evidence confidence

This is a bounded, read-only review of all 46 current acceptance rows through EV-136. It does not change the matrix. I inspected the current matrix and status history, the focused VR-013 and VR-042 reviews, the human-review v3 remediation, and the EV-135/136 Eve evidence during the immediately preceding independent audits. I did not reopen every historical EV-001–132 receipt. Accordingly, **verified receipt fact** below is reserved for VR-043 and the recently inspected Eve lineage/report receipts; other evidence descriptions are **current-ledger claims** whose cited receipts still need the final auditor's hash/command validation before promotion.

The useful completion unit is a shared gate, not a row. Re-running isolated package tests would add little. The remaining work concentrates in human gold/adjudication, full multimodal selector coverage, full paired benchmark semantics, Cloud/remote rollout, and final matrix audit.

## A — sufficient whole-row evidence

| Row | Assessment |
| --- | --- |
| VR-043 | **Already correctly proved.** EV-130's independently audited 29 assertion paths, installed browser navigation, exact offsets/digests, manifest and bundle closure match the whole row. Do not repeat this proof unless the frozen-v1 output changes. |

No other row has a whole-row case in the current record. EV-136 materially strengthens VR-024 but explicitly leaves live Eve inference, native negatives, cancellation and recovery open.

## B — one bounded local closure can complete the row

| Rows | Single closure to build and retain |
| --- | --- |
| VR-002 | One end-to-end override-attempt suite proving deterministic rejection precedes semantics and policy/model output cannot reverse it. Reuse existing unit/property fixtures and native worker custody. |
| VR-003 | One native wrong/same-deployment rejection proof across contract, DB constraint and engine, with a valid control. Existing identity custody can be reused. |
| VR-004 | One accepted-result byte replay plus registered-capture tamper failure through the native read/replay path. Existing CAS/signature audits supply the positive custody side. |
| VR-005 | One consolidated selector/display separation adversarial suite covering ambiguity, ellipsis and source drift for every currently admitted selector family. |
| VR-009 | One declared supported-operation inventory run with deterministic numeric/unit/period/entity/computation replay at the specified effective-100% threshold. Do not broaden the inventory during proof. |
| VR-013 | One credential-free native capability/injection proof: inspect the effective catalog and show prohibited prompt/tool attempts cannot access shell, filesystem, network/search, GUI or delegation. EV-136 supplies the real Eve catalog/KS path; it does not supply these negatives. |
| VR-016 | One native projection-size/retention test binding raw provider response and precontext outside model context to the compact canonical evidence/manifest. |
| VR-018 | One cross-surface projection test showing provider, harness, quality and policy failures remain distinct in API and dashboard. Existing typed worker fixtures should be reused. |
| VR-027 | One exhaustive registered-artifact/orphan/collision/round-trip audit over a sealed native run. Existing CAS closure auditors are the right base. |
| VR-029 | One sampled signed-manifest secrecy audit plus hostile secret/private-reasoning redaction cases. Avoid another generic schema test. |
| VR-035 | One optional-signature enabled/disabled and tamper test over the retained high-assurance manifest path. |
| VR-039 | One generated-report conflict-set fixture for the known count/product/turnaround conflicts, asserting both sides and qualifiers remain visible. |
| VR-044 | One complete generated-output safety scan with adversarial advice/qualification fixtures across HTML, Markdown and JSON reports. |
| VR-046 | One acquisition-policy/source-manifest audit proving gated sources stay unavailable without authenticated user action, including form/terms/auth bypass negatives. Existing unavailable sample-report evidence is a useful control. |

These are the highest-value local closures because each can finish a row without a new provider cohort, new captures, or remote deployment.

## C — requires human/infrastructure input or an already-authorized live external execution

| Rows | Decisive external boundary |
| --- | --- |
| VR-008, VR-010, VR-011, VR-014, VR-019, VR-020, VR-021, VR-038, VR-040, VR-041 | Human-atomized gold, adjudication and qualification-preserving labels are absent. The 180-row v3 pack is explicitly blank and unauthenticated. These rows should share one approved annotation/adjudication campaign and one paired statistical report; synthetic/model labels cannot close them. |
| VR-015, VR-017 | Live Interfaze safe fixtures are executable without another permission request when the configured provider capability is present; the user already authorized cost-efficient testing. Authoritative ZDR/provider-data-policy evidence still requires provider/account capability or policy records. Local adapters and headers alone do not establish provider behavior or policy. |
| VR-023 | A real Cursor Cloud invocation and manifest inspection is required. EV-133 is an actual local Cursor SDK run and cannot be relabeled Cloud evidence. |
| VR-024 | Live Eve inference is a paid external runtime action already authorized under D-014; it should proceed after native cancellation/recovery and negative controls, without another permission stop. EV-135/136 already cover signed host authority, concurrent binding, real KS worker/Storage and signed output with mock inference. Absence of runtime credentials/capability would be an infrastructure blocker, not an approval blocker. |
| VR-025 | Local idempotency/cancellation/replay/recovery evidence is broad, but Temporal Cloud/production reconciliation remains external. Discovery found zero namespaces; provisioning is the actual blocker. |
| VR-031 | A real scheduled comparison/alert in the deployment environment is required after local drift fixtures pass. A local one-shot does not prove scheduling or alert delivery. |
| VR-036 | Authenticated promotion requires human authority; production/dashboard recovery also needs deployment. Existing launch/cancel/read browser evidence should be reused. |
| VR-037 | Capture custody exists, but license/access approval is a human legal/source-owner decision. Do not infer license from successful acquisition. |
| VR-045 | The immutable proposal/diff mechanics exist, but two registered sources remain unavailable and v2 requires review/freeze. Source-owner decisions and reviewed promotion are external. |

## D — multiple local engineering gates remain

| Rows | Minimum completion cluster |
| --- | --- |
| VR-001, VR-033 | Finish consumer cutover/parity together, prove package graph ownership, then deprecate/remove duplicate prototype algorithms. One proof should cover both rows. |
| VR-006, VR-007 | Complete and adversarially test all admitted text/JSON/HTML/PDF-image/table/media/repository/dataset selectors, then audit every accepted leaf's value, derivation, fragment and lineage. Current refresh evidence does not cover that full family set. |
| VR-012 | Complete separated result/policy truth tables and supported dashboard projections; human authority/world-correctness inputs remain dependencies. |
| VR-022 | Close the declared operation inventory across HTTP/CLI/MCP/worker with one generated semantic-parity matrix. Numerous bounded parity slices do not establish the complete inventory. |
| VR-026 | Prove canonical fresh-chain migration ownership and every consumer's generated-type pin. Local migration 141/contract 0.2.36 is evidence for the current slice, not every consumer or remote chain. |
| VR-028 | Complete the threat model and integrated adversarial suite across tenant isolation, SSRF/redirects, hostile files, secret redaction and provider policy. Acquisition negatives cover only part. |
| VR-030 | Produce one disconnected sealed-bundle replay covering hashes, selections, calculations and policy decisions for all declared supported operations. Current metric/benchmark/report replays are bounded slices. |
| VR-032 | Prove the complete dashboard route inventory reads through supported APIs and that every control avoids direct canonical-table mutation. EV-121/122 cover native slices, not the whole dashboard. |
| VR-034 | Final dependency only: after row owners close their work, independently verify every promotion against exact receipts, commands, artifact IDs and authority boundaries. Do not perform rolling “final” audits before inputs stabilize. |
| VR-042 | Complete the offline command's extraction, claim/report verification, adversarial families and deterministic replay. Its required report/ledger/manifest inventory is already independently present; do not regenerate it. |

## Recommended execution order

1. Close the no-cost one-proof rows in B, starting with VR-013, VR-044, VR-046, VR-003 and VR-004 because they protect authority and safety boundaries.
2. Run one human review campaign for VR-008/010/011/014/019/020/021/038/040/041. Reusing the v3 candidate pack avoids ten separate labeling efforts.
3. In parallel, finish selector/leaf coverage (VR-006/007) and the offline full-path command (VR-042); these feed the paired report rather than following it.
4. Bundle cutover rows VR-001/033, transport inventory VR-022/032, and persistence VR-026/027 into three audits instead of row-specific reruns.
5. Prepare all local Eve/Temporal negatives before spending or provisioning; then perform the smallest live Eve, Cursor Cloud and Temporal Cloud proofs.
6. Freeze reviewed v2 and remote rollout last, then run VR-034 once over stable evidence.

Authority boundaries are decisive: model outputs cannot mint runtime lineage or human gold; successful fetches do not grant licensing; local SDK/Temporal runs do not prove Cloud; signed CAS custody proves bytes and lineage, not semantic truth or provider policy.

## Coordinator checkpoint after EV-145 — 2026-09-08

This document's earlier EV-136 classifications are historical. The canonical acceptance matrix now has16proved/21partial/9missing. Newly accepted rows throughEV145 are VR002/003/004/005/006/009/012/013/024/029/032/039/043/044/045/046. Scope corrections: VR006 proves canonical selector families, not fresh raw-modality acquisition; VR045 requires immutable proposals/diff, not approval or freezing of v2; VR012 is architectural separation, not human world-truth labels. These distinctions are recorded in their acceptance aggregates.

One bounded live Eve attempt succeeded: two Luna calls and one KS judge308micros, with signed review disposition and independently verified21artifact handles. Recovery tests are synthetic granted-read/cancel/idempotency controls; native process-death/Temporal recovery stays underVR025. No repeat of this successful provider proof is needed absent a relevant runtime change. Raw/precontext synthetic retention coverage strengthenedVR016, but no live precontext artifact was obtained, so it remains partial. Optional Ed25519 evidence exists, but in-toto/SLSA evidence is absent, soVR035 remains partial under the conservative audit.

Next useful local bundles: typed error API/dashboard parityVR018; exhaustive artifact registration/orphan/collisionVR027; complete cross-surface operation inventoryVR022; canonical package/pin/cutoverVR001/026/033; disconnected full supported-operation replayVR030. Human-gold benchmark/report closure, Temporal namespace and CursorCloud endpoint remain outstanding. Do not repeat finished frozen reports, capture refresh, human-form mechanics or paidEve proof. Do not label local SDK/Temporal evidence Cloud.

## Coordinator checkpoint after EV-147 — 2026-09-08

The matrix now records 18 proved, 19 partial, 9 missing. EV146 accepts VR027 current canonical verification artifact registration after fenced adjudication repair and native independent audit. EV147 accepts VR018 typed execution failures versus completed signed policy outcomes across API/dashboard. Immutable aggregate references were machine-checked after recording (46 and 32 references respectively).

The twelve-mutation admission/worker-lease suite fixed parseArtifact in the shared mutation union, but does not alone prove twelve terminal results. Fastify route `::` escapes a literal colon; it is not a route typo. Installed direct test binaries should be used because normal pnpm invocation can request a noninteractive module purge.

Current local continuation: complete the remaining capture/parse/extraction/replay terminal parity; finish sealed replay breadth beyond two prototype selector shapes and direct extraction-comparison tests; migrate legacy research locator consumers through a KS-owned compatibility adapter with historical coordinate parity. A source hash mismatch in the VR030 r2 receipt requires a preserved correction before acceptance.

Scope issue to review, not yet accepted: VR014 requires support for abstention/human review and policy/review e2e fixtures. This capability requirement may be provable using isolated, explicitly synthetic review identities and fixtures; it should not automatically be equated with the genuine human-gold campaign needed by benchmark/semantic-accuracy rows. Current policy tests cover critical abstention and review; durable request packets have independent native evidence, but complete review e2e closure still needs an audit. Do not fabricate or import actual human labels or authorize production admission.

## Latest local continuation — EV148, 2026-09-08

19 proved / 18 partial / 9 missing. Native parse and registered-HTML capture parity now supplement VR022; its remaining terminal inventory gaps are verifyExtraction and replayRun. Root independently audited parse operation uniqueness, success receipt/event fence and CAS bytes. Native parity progress: internal/verification-native-parity-progress-20260908.json.

The prototype locator facade now imports the KS package API through an explicit local link with frozen lock consistency and passing historical tests. Legacy verifyBundle remains unmigrated. Canonical progress ledger is SW-13-PROTOTYPE-CUTOVER-LEDGER.md; the agent's initially misplaced `KS docs` ledger is retained as history. Progress: internal/verification-prototype-cutover-progress-20260908.json.

Do not rerun EV146/147/148 recorders. EV148 accepted disconnected sealed replay after root's independent six-test rerun; final R4 source is snapshotted. Earlier R2 hash mismatch and narrower coverage are retained history. All work in this continuation used zero new paid provider calls. Real human labels, Temporal Cloud namespace, and the pending temporary Cursor Cloud endpoint approval remain separate prerequisites. No completion or blocked goal status was set.
