# Generated-report semantic completion — root progress — 2026-09-08

Previous goal turn: verified progress (EV162), not a wait or no-progress turn. Goal remains full specification completion.

## Current findings and corrections

- Current EV162 source/evidence were inspected before continuing. Its native claim/report calls executed and replayed, but the offline dependency factory omitted `projectionSelectorResolver`; all 40 claim results therefore contained `SELECTOR_RESOLVER_ADMITTED` failures. EV162 integrity and execution counts remain historical facts, not proof of successful native claim mechanics.
- Root added the admitted projection resolver to the shared offline factory and strengthened the v1 regression to require all 40 native assertions pass mechanics. The regression passes on the corrected source. EV162 sealed snapshots and output are preserved.
- Report-wide consistency failures remain intentional and block ordinary semantic admission. Sol is adding a narrowly scoped diagnostic semantic preparation path over privately retained, valid base assertion mechanics. It must preserve final failed report status and reject pointer, custody, identity and qualifier failures. No ordinary semantic batch/policy promotion may be created from diagnostic observations.
- A bounded report run will cover 29 individual report assertions, never upload whole reports, and preserve each exact ledger/report identity. Existing 38 v1 semantic observations cannot be attached to those different contexts.

## Spend and input preflight

Root applied the Vercel AI Gateway skill and fetched current official docs. Live model discovery confirms Luna base input/output prices of USD0.20/1.20 per million tokens. `internal/verification-report-semantic-preflight-20260908.json` records current catalog and prior cohort evidence: 105,951 microdollars prior commitment plus 10,000 retained Eve Cloud unknown liability plus the proposed 200,000 maximum gives 315,951 microdollars, below D014's USD25 cap. This is a preflight, not a reservation or provider execution.

Existing D013 bounds remain: one public assertion plus one necessary selected fragment; at most 2,000 UTF-16 case characters, 10,000 serialized UTF-8 bytes, 900 output tokens, temperature zero; serial calls, no tools/search/redirects, no automatic retry, stop after three consecutive failures. A durable exact-input grant/startup reservation must be retained before dispatch. Human labels, source-rights and sensitive-input vendor acceptance are not fabricated.

## Parallel retained-extraction mapping

Luna mapped v1/v4 content identities: 21 exact assertion/fragment matches, 20 with retained field profiles; 19 v1 cases have no exact v4 match. v1 does not have an equivalent structured field-plan artifact. This informs targeted completion but does not promote pilot-v4 evidence to v1.
## Prepared report diagnostics and dispatch safeguards

The exact EV162 report preparation has 29 assertions (11 TruDiagnostic, 9 Generation Lab, 9 comparison), 43 exact local claim/ledger handles and three report handles. Receipt `internal/verification-EV162-generated-report-semantic-plan-20260908.json` SHA256 `57cf1e26e48dcaef65a851be8e9b7b0b49fe64ed6a896ff1a2d9a7f1a9b93045`; plan digest `sha256:4476f27ca81a0b5416c215dcb6284d6353ea96eb4347778ee5338bf9bfa68b3b`. Maximum content 1,476 characters and request 4,042 bytes. No provider calls at preparation.

Root hardened the new executor with recursive immutable plans, single-use dispatch claimed before awaits, exact grant schema and limits, rejection of duplicate replay entries, and immediate per-assessment persistence outside provider-error catch. New mocked composition tests exercise these boundaries (four passed); they do not substitute for live or trusted-custody replay. Strengthened native service regression additionally rejects diagnostic results as ordinary semantic batches, changed operation contexts and changed final results (six passed). Related claim/report/custody tests passed 12/12 before this additional regression, and current application typecheck passed. Terra's initial two tests did not reach the duplicate-entry guard on an authentic preparation; root replaced that file with the stronger four-test fixture.

Sol is finishing the protected live runner and retained response replay. Root review required exact sealed input-file hashes, durable startup reservation/global single-use lock, unknown-cost accounting, and complete artifact/replay validation before execution. No new dispatch yet.
## Installed correction verified (EV163 candidate)

The rebuilt installed CLI ran from the parent workspace with credentials removed and fetch disabled. All 40 native claim results now pass (the proof asserts their passed disposition); three reports and four mechanical mutation families execute and replay, with 38 existing semantic replay cases and 29 hash-checked files. Duration 25,938 ms, expected exit 2. Receipt `internal/verification-offline-demo-EV163-20260908.json` SHA256 `3cb0b7118b693ea69f8f1439e82f971c400f040b6050b6869359dda9fdbfaaef`. This corrects EV162's missing resolver without overwriting EV162. Report semantics are still a separate pending live/replay checkpoint.

Luna's independent diagnostic-path review passed the service bypass, normal admission and dispatch controls (10 focused tests passed). Root corrected tenant and descriptor binding for local handles. Generic source provenance metadata remains a trusted engineering-composition input, explicitly documented; the concrete EV162 preparer validates it against the sealed catalog and registered capture data. This helper is not exposed as an HTTP admission endpoint.
## Actual bounded Luna report run and offline replay

The authorized run completed 29/29 requests with zero failures, all model identities matched, and 9,572 microdollars reported known cost (USD0.009572); no unknown-cost records. The full USD0.20 launch reservation remains conservatively recorded for future cohort accounting. Do not launch the grant again: both root startup reservation and plan-scoped runner lock are immutable single-use records.

Live receipt `internal/verification-EV162-report-semantics-live-20260908/receipt.json` SHA256 `6d38d6a74c28f848823ecde39002d5d1db3bf95ead4a4a5acf4b73564f524b8b`. All 29 assertion judgments are directly_supported; their individual semantic disposition is admit, but they are diagnostic-only and cannot change the report-wide failed disposition or create an ordinary service-issued assessment batch. No human label, authority/applicability, cross-family judgment or acceptance promotion is claimed.

Root independent audit checked exact approved request wires, 87 artifact references (77 unique files), all digests/lengths, raw/output equality, observed model/input identities, serial journal and reported cost. Receipt: `internal/verification-report-semantics-independent-audit-20260908.json`. Root then ran the separate replay with the Gateway key removed and global fetch disabled; all 29 assessments matched with zero external requests, receipt `internal/verification-EV162-report-semantics-replay-20260908/receipt.json`. The first attempt at this offline command failed before script execution because Windows ESM requires a file URL for --import; the corrected file URL succeeded, with no network calls in either attempt.

Current integration: package the exact capture in a portable sealed fixture and include it in the installed offline demo. The fixture retains the original EV162 run digest; a new demo must show its own run digest separately and prove exact report/ledger handle equality. Full demo/provider coverage and human-gold quality remain unfinished.
## Final integration validation

The portable e54beb fixture is now copied into installed CLI assets (77 additional files), authenticated by the loader and bound to freshly rebuilt native report/ledger inputs. The real v1 application integration test passes with 29 report diagnostics and unchanged failed report status. Application and CLI typechecks/builds pass. The installed integration receipt is `internal/verification-offline-demo-EV163-report-semantics-20260908.json`, SHA256 `bf9c2a73701a981fec6d2f00ba33f712edae9b0955ef0ca5b5b1ad222c14efab`, 47,171 ms, 30 hash-checked files.

After that receipt, a final loader guard was added to reject file-size mismatches before reading bytes; its two focused tests pass. The application was rebuilt with that guard. The installed CLI test then hit the previous 60-second child timeout under local load (no assertion mismatch). No leftover diagnostics/Vitest process was found in the process inventory. The now larger end-to-end test has a bounded 120-second child / 130-second outer limit; the failing installed case was rerun and passed in 55.09 seconds (six unrelated option/tamper cases had already passed). This final test verifies all 40 native claims pass, all three reports retain consistency failures, all 29 diagnostic replays are present, original/current run IDs differ, and all 30 output hashes match. No new provider calls.

This is a progress checkpoint, not full mission acceptance. Keep the goal active; next work is remaining structured/provider and semantic coverage, followed by the deferred authenticated human/rights/vendor and benchmark acceptance gates. Do not rerun the successful paid report plan or attach its results to different report/ledger identities.
