# SW-12 live Eve audit progress

The independent live-Eve audit is complete. The retained proof supports two bounded Luna gateway responses, one Eve-authored report-verification operation (`bf24f4b2-d856-57cc-a1bc-89a0f8fd88c8`), a `review` disposition, and one settled KS provider attempt at 308 micros.

The local-CAS custody supplement verified the actual signed manifest with the existing `createEd25519Verifier` and `inspectAuditBundle` APIs. It read 26 objects from local Storage only, verified the signed manifest plus all 21 registered input/output handles against their declared byte lengths and SHA-256 digests, and made no storage writes or provider calls. Its focused 38,724-byte signed-manifest review found none of the configured credential/private-key markers.

The isolation receipt records that the disposable proof database and dump directory were absent after cleanup. The only source-snapshot mismatch is the intentionally expanded recovery test file; it is pinned by the later 12-test recovery-control receipt and does not indicate runtime source drift.

VR-024 can be promoted only for the exact bounded Eve-authored-tool and recovery row when joined with EV-135, EV-136, EV-137, the live proof, the custody supplement, and recovery control. VR-029 is supported for the sampled signed manifest and the hostile private-field fixture, not for every CAS object. Broader recovery remains VR-025 scope.

Consumer cutover inventory: [internal/verification-consumer-cutover-inventory-20260908.json](../../../../../../internal/verification-consumer-cutover-inventory-20260908.json) records directly computed hashes. Canonical DB is 0.2.36; Knowledge Services and research contracts now point at the same verified local 0.2.36 tarball (`f62ebe13e042264e98518696820c41e4df5083a96f6c58e9988810588aadb218`). The research lock resolves 0.2.36 and frozen offline lockfile consistency passes. The compiler's installed package remains 0.1.0 because the update intentionally used `--lockfile-only`; direct tar-source export validation covers the seven imported DB types instead.

The lockfile-only refresh synchronized many already-drifted workspace importers, not only the DB pin. The full current lockfile is preserved at `internal/verification-consumer-cutover-pnpm-lock-preflight-20260908.yaml` for review; do not present its broad diff as a narrow dependency-only change. Mission Control still uses a dated vendored `knowledge-contracts` runtime whose JS differs from current KS runtime while its declarations match. Refresh that vendor only after the Mission Control dispatch compatibility suite passes. The old generic `verification-core` algorithms are currently observed only in research experiments and remain outside active KS-backed Eve verification.

The target compiler receipt, `internal/verification-consumer-target-typecheck-20260908.json`, records a successful isolated TypeScript compile with `@aiengineer/database-contract` path-mapped to extracted 0.2.36 tarball sources. It does not rely on installed node_modules, which correctly remains at 0.1.0 after the lockfile-only update. `internal/verification-mission-control-vendor-contract-review-20260908.json` records 52 passing worker dispatch tests and one passing kernel dispatch test. The current KS contract runtime adds seven exports and removes none; Mission Control imports none of those additions, so no vendor refresh was made.

VR-018 interim independent source audit: `internal/verification-vr018-failure-projection-independent-interim-5e9ead2d-42df-4fe2-a704-f73a26036585.json` (SHA-256 `8f642d97e79942efd6c6c201851083e378bdd92071e11ccc95a439a0e7a88df4`) computed all input hashes before writing. It verifies the compact receipt-derived failed-operation projection, its recovery clearing assertion, and the bounded 12-mutation HTTP/CLI/MCP/lease inventory. It is deliberately **not accepted**: dashboard still has the fictitious `SEMANTIC_DISPOSITION_REQUIRED` operation-failure fixture while canonical semantic reasons belong to completed sealed policy results. The old 4/4 API log is explicitly stale after the source became a three-actual-class parity case; no provider, CAS, remote, database, or test execution was performed by this audit.

VR-018 corrected independent audit: `internal/verification-vr018-failure-projection-independent-final-ab8f81a7-a148-49ef-ac6b-0b128d9d5944.json` (SHA-256 `2f4ec21b27302c566ae55594e4b04a6d1859247e6ed7dc7e49826e71f462a90e`) recomputed source hashes after the correction. It confirms the synthetic semantic operation-failure fixture is absent, while the typed sealed terminal policy projects the canonical `SEMANTIC_HARD_FAILURE` and `SEMANTIC_DISPOSITION_FAIL` reasons. It also confirms failed-only canonical receipt authority, compact failure fields, recovery clearing in current API source, and strict `parseArtifact`/twelve-admission inventory scope. Dashboard’s forthcoming signed-policy tamper log is separate evidence; this audit ran no tests or external calls.

Consumer fresh-chain closure: `internal/fresh-chain-audit-vr026-0236.json` applied the current 141 canonical DB migrations in a new isolated local workspace, independently read ledger `141|20260908010000`, and stopped only its own container. The machine audit `internal/verification-vr001-026-033-fresh-cutover-audit-91d89b84-b8f6-4e30-96ac-3815daca87b7.json` (SHA-256 `de5866d1056b9a20fd55dd437a4c8ae1924c2d2372f67fa465f77a9d322d45b5`) hashes the fresh proof, current 0.2.36 pins, and isolated tar-source consumer typecheck. This closes the current local fresh-chain gap for VR-026 evidence only. VR-001 and VR-033 remain partial because the research-only legacy algorithms and the Mission Control runtime vendor compatibility/refresh gate remain unresolved; no matrix row is promoted.

Exact VR-026 consumer audit: `internal/verification-vr026-all-consumer-pins-audit-f4350386-693a-49df-a5a6-1ecc4b9167b5.json` (SHA-256 `b73475d746432e1bd1f13ff339fa49fa98a0dae7d22c88a40b3d68e4f1259e6e`) enumerates current direct database-contract manifest/lock consumers. Canonical owner is 0.2.36 with the 141-migration fresh-chain ledger. KS persistence and research contracts are pinned to the verified 0.2.36 local tar. The research installed junction now targets the verified extracted 0.2.36 package and normal consumer `tsc --noEmit` passed; its prior 0.1.0 junction remains at `packages/contracts/node_modules/@aiengineer/database-contract-0.1.0-preserved-20260908` for rollback. `agents_dashboard` retains a Git/0.1.0 active pre-research-types pin and industry-cloud research retains vendor/0.1.0 schema-tooling pin, so no workspace-wide pin claim is made. `fresh-chain-audit-vr026-0236.json` was newly created, not overwritten; its immutable SHA-256 is `0d64404e2c4ca61c217fb63c4caa0eac356ff54db2ac8ee1be49a879f68c3aa0`.

Final VR-018 independent acceptance: `internal/verification-vr018-final-independent-acceptance-88fd2fd1-f16b-4f99-8034-9d178e631a46.json` (SHA-256 `531f0b8b2f8c57959b4bbaf1798f5ae33098529a8a5aba2cf388ef9caf85bfc4`) programmatically matches every R2-declared source/log hash except the explicitly later root API routes negative. It separately pins the current routes test, corrected `openapi-parity.test.ts` name, and refreshed API R3 10-test log. It verifies corrected contract/DTO outcome equality, R2 dashboard 37-test log, and the retained native signed-policy read’s 13 checks, read-only transaction, and zero provider/parser dispatches. Recommend VR-018 acceptance only for the exact typed failed-operation and sealed completed-policy dashboard/API projection; no full worker/deployment/code-inventory claim.
