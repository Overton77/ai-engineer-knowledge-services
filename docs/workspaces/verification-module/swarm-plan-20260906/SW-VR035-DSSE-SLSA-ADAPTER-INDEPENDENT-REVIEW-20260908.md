# SW-VR035 DSSE/SLSA adapter independent review — 2026-09-08

Scope: read-only review of the authored pure-core adapter against `VR035-ATTESTATION-BUILD-TYPE-20260908.md`. No provider, database, CLI, or full-suite execution was performed.

## Finding

The core path is structurally aligned with the proposed optional capability. `createVerificationDsseSlsaAttestation` clones/freezes the audit bundle and snapshots the trusted binding and signer identity before asynchronous work (`packages/verification/src/provenance/attestation.ts:176-196`). It independently requires a verified existing audit seal (`171-174`), derives the builder from the manifest deployment (`94-96`), and requires the exact server-owned builder/key pair (`98-101, 188-189`). The subject is reconstructed from the canonical signable audit payload (`128-134, 149-152`), and inspection compares the complete canonical in-toto statement with that reconstruction (`220-227`). PAE uses UTF-8 byte lengths (`117-125`); payload/signature decoding is bounded and rejects noncanonical or mixed alphabets (`103-115, 199-217`). Exact-key checks and statement equality exclude private or arbitrary fields (`207-225`). The focused tests cover valid creation, URL-safe payloads, payload/type/subject/dependency/private-field tampering, unsigned bundles, changed bundles, and mismatched bindings (`attestation.test.ts:44-86`). The package exports the capability additively (`provenance/index.ts:7`, root `index.ts:8`).

Two concrete items remain before this can be treated as a complete VR035 implementation proof:

1. **CLI proof/path is absent in the inspected tree.** No Mission Control or Knowledge Services CLI source currently references these exports. Therefore the required built read-only export/inspect path, environment-only key loading, `wx`/no-overwrite sidecar behavior, and immutable-output proof are not yet evidenced. This is an integration/proof gap, not a reason to change the core contract. The smallest next action is to add the optional CLI command around the exported functions, load signing material from process environment only, write with exclusive creation, and add one synthetic export→inspect plus existing-file rejection fixture.
2. **Export does not canonicalize the signer’s returned encoding.** `decodeBase64` validates either standard or URL-safe input, but line 195 places the original `signatureBase64` into the envelope. A generic `AuditBundleSigner` can therefore produce a URL-safe signature in an object documented as a standard DSSE JSON envelope. Normalize the decoded 64 bytes to standard base64 for exported `sig` (inspection may continue accepting both alphabets), and add one signer-returned-URL-safe regression assertion.

A bounded-input edge remains worth closing in the same core proof: string/byte envelopes are capped at `MAX_ENVELOPE_BYTES` (`199-204`), but a caller-supplied object bypasses that byte cap before recursive exact-key/canonical processing. Add a bounded object-envelope normalization or explicitly constrain the public API to bytes/string at the CLI boundary; include an oversized/deep object rejection test. This is the only additional bounded-read concern found; no source, credential, private-reasoning, or arbitrary-parameter leakage is present in the constructed statement.

No issue was found with builder derivation, signed-bundle precondition, canonical subject binding, DSSE PAE byte accounting, or mutation of the bundle/binding/signer identity across awaits. Existing verification operations remain additive and unchanged by the inspected exports. Disposition: **core semantics provisionally acceptable; VR035 remains pending CLI integration and the two narrow corrections above.**
