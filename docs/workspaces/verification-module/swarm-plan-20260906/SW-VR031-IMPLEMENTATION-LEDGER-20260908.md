# VR031 implementation ledger — 2026-09-08

Implemented the local planning boundary in `packages/application`: its runtime allowlist covers provider, model, parser, grader, and policy; every plan is delegated as one atomic durable action, never a claim followed by side effects.  The explicit disposition is `revalidate` or `review_required`; the latter requires a bounded reason and cannot be interpreted as permission for a paid/provider call.

Added the unapplied canonical migration `20260908030000_verification_drift_revalidation_outbox.sql`.  It derives eligibility from an existing revalidation-required semantic observation and its exact source operation, stores one tenant-scoped immutable plan per observation, uses an expiry-reclaimable fenced claim, and exposes only plan/claim/ack functions.  It enables RLS and grants no table mutation privileges to runtime roles.

Validation completed: application focused tests 4/4; persistence adapter focused test 1/1; application and persistence typechecks pass.  The MC Temporal consumer requires the approved internal KS claim/ack API route and authenticated client adapter; implementation is in progress and no provider or remote call has occurred.

The KS private consumer boundary is now implemented at `/v1/internal/verification/drift-revalidations/{scan,claim,ack}` and `/v1/internal/verification/drift-alerts`. It is absent unless `VERIFICATION_DRIFT_REVALIDATION_ENABLED=1` and the strict `VERIFICATION_DRIFT_CONSUMER_SERVICE_IDENTITIES_JSON` allowlist are configured. Every call also requires the dedicated explicit scope, a service actor, and an exact allowlisted service identity. Scan creates review-required model-drift alerts only; it never accepts a caller-supplied dispatch request. API endpoint tests pass 3/3, API typecheck passes, and the persistence package builds.

The HTTP boundary projects only strict compact response DTOs. Claim and inbox records validate UUID identity fields, the five-dimension allowlist, bounded uppercase reason codes, review-only disposition, and ISO timestamps; unrecognized runtime fields are stripped before serialization. Focused endpoint validation is now 4/4 plus API typecheck.

Component comparator mapping: signed audit bundles are inspected through `inspectAuditBundle` with a configured verifier and are bound byte-for-byte to their admitted manifest artifact handles. `VerificationRunManifest` supplies provider endpoint identity, provider model, parser version, grader version, and policy version. The comparator emits a registration-ready immutable observation containing only both manifest references, derived component identities, sorted changed dimensions, and its canonical payload digest. Missing provider/parser/grader, an unsigned/invalid bundle, artifact-byte mismatch, or cross-tenant inputs fail closed.

OpenAPI parity now documents the four service-only drift routes, their bounded request/response DTOs, and their tenant-bound bearer/service-identity requirement. Generated contract artifacts were refreshed. OpenAPI parity passes; API suite passes 139 tests with 5 intentional skips using two workers; API typecheck passes.

Component-monitor runtime tests cover incomplete configuration, foreign-tenant monitor isolation, unchanged signed-comparison result suppression, durable publisher planned/already-planned accounting, and the aggregate scan limit shared with model observations. The runtime uses injected trusted-resolver and publisher ports; no database cast or caller-selected dimensions are used. Focused runtime tests pass 5/5 and API typecheck passes.

## Root integration review and bounded verification
Root fixed outbox SELECT grants under tenant RLS (migration e40493c4cd0f0872df55670b3beb49bec7bf271fd443aa41ad280cdba258ef9d), then ran claim/reclaim/stale-token/ack/cross-tenant checks as executor_service in a rolled-back transaction. Receipt: internal/verification-drift-revalidation-outbox-sql-r2-20260908.json. No migration applied. Positive semantic planning remains untested because retained revalidation-required rows are absent; synthetic outbox seed is explicitly engineering-only.
Root added explicit grant-scope enforcement even for allowlisted knowledge_admin service actors, and corrected the route test identity to canonical mission_control_client. Focused route5 tests/typecheck pass after that fix. Terra completed OpenAPI parity and full API139 passed/5 skipped with two workers before the additional admin-scope test. MC production drift consumer/workflow and paused schedule setup code pass focused5/full57 worker tests, typecheck/build. No actual schedule is deployed. The five-dimension comparator now resolves registered audit-bundle bytes and verifies signed snapshots, but root corrected a runtime structuredClone(function) bug; real signed fixture tests are in progress with Luna. Comparator is not yet integrated into the model-only durable intake. No full VR031 acceptance.

## Component monitor production composition and fair scan cursor

The API runtime now composes `PostgresVerificationComponentDriftPublisher` with a fresh tenant-scoped `PostgresVerificationRepository` and the configured verification storage bucket. Its artifact authorization permits only the scan tenant and `verification_replay`; monitor dimensions remain derived by the trusted comparator rather than configuration or request input. Component monitoring stays disabled unless both strict monitor and public-key configuration values are present, and storage credentials are required only for that enabled path.

Component scans maintain a bounded in-memory cursor for each configured tenant. A scan evaluates at most the capacity left after model observations; it advances after each successful trusted comparison, including unchanged or durable-replay observations. A comparison failure leaves that pair at the cursor for retry. With at most 100 configured monitors, the cursor state is bounded by server configuration and a process restart safely resets it because publication is idempotent.

Validation: `verification-drift-revalidation-runtime.test.ts` passes 6/6, including three configured monitors scanned in three successive limit-one calls; `@aiengineer/knowledge-persistence` build and API typecheck pass. No provider call, migration application, or remote mutation occurred.

## Dashboard drift inbox independent review

Reviewed the Mission Control dashboard drift inbox: the browser allowlist admits only `GET internal/verification/drift-alerts`; `scan`, `claim`, and `ack` have no browser route. The generic proxy retains signed-session tenant and mission headers plus the existing server-held knowledge token, while the drift DTO only projects alert ID, evidence artifact ID, source operation ID, changed dimensions, reason code, and publish time. Lease tokens, owner fields, object paths, and credentials are excluded. The UI accurately distinguishes the unloaded view, bounded latest-100 result, empty tenant inbox, and unavailable service state.

Added one mocked Playwright proof at Mission Control `apps/dashboard/e2e/drift.spec.ts` which exercises a published row, empty reply, and unavailable reply through three GET inbox reads and asserts no request body or mutation method. `playwright test e2e/drift.spec.ts --list` discovers the single test; no dashboard server or build was started.

## Final independent VR031 verdict

Immutable verdict: `C:\Users\Pinda\Proyectos\aiengineer\internal\verification-vr031-final-verdict-20260908.json` (SHA-256 `b5a698dcc695b9162ab7ca75eaac1be5d6e19e9574227b5b0451aeb810de2989`). Status is **engineering path proved; deployment activation pending**.

The native proof binds two actual signed audit-bundle artifacts to admitted verification runs, derives provider/model/parser/grader/policy changes from their signed bytes, registers one immutable observation with both parents, derives the candidate operation in SQL, publishes one review-required alert, and returns already-planned without a second alert on exact replay. The local rollback-only DDL proof separately verifies tenant fencing, invalid hash/run/tenant rejection, custody lineage, claim, acknowledgement, and inbox visibility.

The independently read scheduled Cloud receipt `verification-component-drift-scheduled-cloud-f80000a4-cfbb-48bf-bda3-68150b1bb9a2.json` has SHA-256 `d809b625938c499943f531a4e96809bb1a3be0a33d98e53c8cb067e4c00da638`; its referenced history hash recomputed exactly as `5a1aba15f81246257a2301634e8199faaea7301f59d414e9af67c7797895802c`. It records a paused real Temporal Cloud schedule manually triggered once, completed with one planned alert and zero provider calls, retained paused state, replayed its history, and deleted its disposable schedule. The corrected timestamp-suffixed workflow identity is present. Root confirms the preliminary owned schedule/workflow was cleaned; no active scheduled job or provider work remains from that correction.

Remote rollout evidence records migration 209 at `20260908214621`, both drift tables RLS-enabled and empty, and unchanged retained provider-attempt/budget fingerprints. With zero remote drift rows there is no remote custody/function execution to inspect; the local rollback proof is the evidence for those fenced behaviors. Remaining work is operational: configure monitor/key pairs in the deployed API and create an intentionally enabled long-lived schedule. Current review-required policy correctly made no paid revalidation call. The fixture does not establish quality, promotion, source authority, calibration, or human-review completion.

## Scheduled proof successor

The final paused Cloud schedule receipt supersedes the preliminary scheduled receipt: `C:\Users\Pinda\Proyectos\aiengineer\internal\verification-component-drift-scheduled-cloud-0eae0bd3-6771-468b-a646-c70d4603cb2c.json` (SHA-256 `ff87467802306921b8cd971520ed4db1b9737efd694b0883917966b7fb8f1e14`). Its adjacent history recomputes to `efd2ce7aec37d02ae3af55d4a476f77c202aa193ad18d60944bc27526aefc363`: six decoded payloads, with all three checked credentials absent. The schedule was paused before and after its single trigger, completed planned=1/alertsPublished=1/providerCalls=0, replayed history, and was deleted; the direct replay reported alreadyPlanned=1/alertsPublished=0.

Immutable successor verdict: `C:\Users\Pinda\Proyectos\aiengineer\internal\verification-vr031-final-verdict-20260908-r2.json` (SHA-256 `daee88d3fe86900627ccd93a2dba13aee13ea0cf8bec0c3343a97a049b14b64d`). It supersedes the earlier verdict only for the final scheduled-proof reference. Engineering acceptance is complete; persistent server monitor/key configuration and intentional activation of a long-lived tenant schedule remain rollout configuration, not an unimplemented engineering requirement.
