# Remaining engineering after EV159

**Superseded by EV161 engineering handoff:** all five finishable work items below are closed with explicit evidence limitations. The supported dashboard command surface is accepted; actual Eve primary lane, registered descriptive comparison and Interfaze capability/retention are complete. Human/vendor/source-rights decisions and the sealed quality benchmark remain. See `../ENGINEERING-HANDOFF-20260908.md`. The original task list is retained below.

EV159 accepts Cursor Cloud invocation, Temporal lifecycle behavior and drift monitoring. It does not complete the whole Consumer Proof Harness described in `02-INTEGRATION-AND-TESTING-MODEL.md` section3 or the entire dashboard requirement. Current matrix:28 proved,11 partial,7 missing. The aggregate's40 file hashes were independently verified; integrity receipt is `internal/verification-cloud-drift-EV159-integrity-audit-20260908/receipt.json`.

## Finish before requesting human review

1. **Dashboard durable controls:** launch/cancel exist. Expose the existing KS selected-run replay operation through the authenticated server boundary and test authorization/recovery. Inventory retry/reconcile and human-controlled promotion/suspension against actual existing commands; do not add a second Mission Control kernel. Terra owns replay implementation.
2. **CPH receipt view:** `/verification/harness` still explicitly reports unsupported. Add a compact tenant/mission-scoped supported server projection and render provenance/real-versus-fixture fields. No request may select arbitrary local files, expose raw transcripts or invent successful receipts. Luna owns this bounded implementation.
3. **Eve primary orchestration lane:** retained actual Eve proof uses direct KS, the explicitly degraded lane. Complete bounded authored-tool transport through existing MC launch/Temporal Cloud while preserving producer/verifier identity and keeping all bearer/signing credentials out of workflow histories. Existing Eve->KS acceptance remains valid; it does not prove this missing path. Sol owns the trust-boundary design following its narrow provider audit.
4. **CPH paired evidence:** section3.4 requires registered restricted transcript, report/assertion ledger, operation and workflow IDs per lane, then an actual benchmark-comparison artifact over identical frozen inputs. Reuse retained evidence where inputs match; do not fabricate a paired experiment from different inputs or claim engineering outputs are human gold.
5. **Interfaze evidence:** verify official fixed-task/precontext capabilities, prepare one bounded safe fixture if supported, and distinguish unavailable native fields from failure to preserve returned fields. Sensitive-input vendor-policy acceptance remains external; no sensitive probe is appropriate.

## Deferred team inputs

Human identities/annotations/adjudication, source-rights disposition, sealed quality benchmark and human promotion authority remain with the team. The180-case review pack remains blank. These inputs are not required to finish the engineering above, and the user should not be repeatedly asked for them while that work remains.

## Cost and resource discipline

Do not repeat accepted Cursor/Temporal/drift proofs. The completed Cursor cohort used three Luna runs, USD0.04202667 total including EV158; no Gateway calls occurred in EV159. This excludes earlier cohorts, Temporal infrastructure and coordinator usage. The temporary endpoint is closed, Cursor agent archived, and disposable workers/schedules stopped/deleted. Any remaining actual agent run must use a narrow fixture, recorded limit and exact cleanup. Preserve dirty worktrees and immutable receipts.
