# Remaining goals and exit evidence

This is the continuation backlog, not a reduced definition of success. The source specification sections3,7,15,17–29,33–34 and all46 acceptance rows remain in scope. Consult ACCEPTANCE-SNAPSHOT.md for verbatim requirements and required proof. All rows were partial/missing at handoff; no percentage completion is defensible from green package checks.

## Recommended execution order

### 1. Finish extraction resilience and supported evidence coverage

Use the existing real child-process harness to test partial response retention, candidate/provenance retention and cancellation windows. Inspect recovery paths before adding code; preserve original provider attempt identity, full artifacts and accounting liability. Extend arrays/dynamic evidence only with per-leaf native source binding and selector coverage. Do not simply relax the fixed-leaf admission gate. Exit: exact final native state and readable signed publication where appropriate, no duplicated provider call, no fabricated candidate or response, meaningful crash/cancel tests and audit.

### 2. Complete semantic/public use-case inventory

Inventory all twelve specification use cases against actual application, worker, contract and public surfaces: captureSource,parseArtifact,extractStructuredData,verifyExtraction,verifyClaims,verifyReport,verifyMetricObservation,runBenchmark,compareBenchmarkRuns,replayRun,inspectAuditBundle,requestAdjudication. Existing helpers/contracts do not prove an admitted end-to-end use case. Prioritize verifyClaims/verifyReport composition with trusted native evidence custody, independent verifier identity, qualifier-preserving report offsets, separate authority/correctness/support/faithfulness/provenance dimensions and deterministic policy. Human adjudication needs an authorized bounded queue, conflict handling and immutable reviewer decisions. Exit: true per-use-case cross-surface worker evidence and prohibited-authority tests.

### 3. Finish case/score and export/replay completeness

Audit canonical evaluation dataset/case/label/variant/grader identities and immutability. Ensure all generated case and score outputs are persisted with engineering-label provenance rather than implied human approval. Extend disconnected export/replay to remaining operations with actual artifact bytes, native selectors/calculations/policy and signed manifest. Test missing/corrupt/unsupported-version evidence and source/runtime drift. Exit: a sealed bundle can replay the promised scope without network or workspace-only files.

### 4. Complete diagnostics-company benchmark and reports

Read WS-07-INDEPENDENT-ACCEPTANCE.md before rerunning paid work. Existing paid pilot:39 cases/117 calls, not final human gold. Existing180-candidate preparation has provisional leakage-grouped90/14/76 split, not180 approved cases. Need reviewed source/license records, atomized facts/claims, independent human labels and qualified review; exact evidence for company/product/algorithm/biomarker/count/system/limitation/publication claims; visible cross-page conflicts; first-party support versus independent corroboration and publication applicability separated. Complete baseline/Interfaze/cascade/consensus/abstention paired arms, repeated runs, calibration, clustered uncertainty, catastrophic errors, slices and review burden. Generate company/comparison/audit reports and machine ledgers with factual assertion-to-fragment navigation. Exit: sealed paired benchmark and reports meet approved gates with human gold and truthful denominators.

### 5. Security, operational readiness and remote rollout

Finish WS-09 threat model/adversarial review across tenant RLS, actor/mission grants, parser isolation, SSRF/redirects, hostile schemas/files/decompression, secret redaction, retention/deletion/data policy and ZDR. Test DB/Storage/provider outage behavior, retry classes and drift revalidation. Review current remote migration history before applying the local221–330 range; this handoff does not authorize skipping compatibility review or inventing remote state. Pin all consumers to canonical contracts and verify native SQL/grants. Exit: clean-environment setup, deployment configuration, health/capability checks, security evidence, rollback and operational runbooks. Existing user authorization covers necessary work, but actual human policy/promotion decisions cannot be invented.

### 6. Complete real orchestration consumers and remove duplication only after proof

Use local Mission Control/Temporal evidence as a starting point. Complete real Temporal Cloud, Cursor Cloud and EVE fixtures with original operation context/capability/deployment identities and bounded tools. Verify cancellation, lost responses, retries, worker loss and reconciliation through deployed paths. Then measure old/new algorithm parity and record rollback/compatibility period before retiring duplicate prototype algorithms. Exit: consumers invoke the service/client, not copied verification code.

### 7. Dashboard, human workflow, shadow gates and final audit

Build supported server/API evidence views first, including original versus reconciled accounting, citations/source fragments, failure taxonomy, benchmarks and review state. Browser code must not directly mutate canonical tables. Add authorized controls only over durable commands. Verify loading/error/empty/denied states, tenant isolation, evidence drilldown, review conflicts and audit trail. Check the intended dashboard stack against its own current repository/specification before scaffolding (prior continuation notes call for Next16 canary/TanStack). Complete actual human review and production shadow pilot against approved selective-accuracy, critical-error, cost, latency and review-volume gates. Final independent audit must match every specification requirement/numbered command/artifact and every acceptance row to authoritative evidence.

## Completion gates that require real external/human evidence

Human gold count was recorded as zero in accepted WS-07 evidence. A model-generated label is not a human annotation. Cloud execution needs actual cloud run/history/capability evidence, not local stand-ins. Supplier billing truth requires authentic evidence/operator reconciliation; synthetic fixtures prove mechanics only. Rights, sensitive data policy, qualified high-risk review and provider/policy promotion must follow recorded decisions. Do not declare the whole mission blocked merely because these remain: continue independent engineering until genuinely at an impasse, then identify the exact missing external action.

## No work to redo without cause

Do not rebuild the reconciliation stack in another package; CLI/MCP now exist. Do not rerun spent pilot budgets or old consumed cohorts to obtain newer timestamps. Do not rewrite all existing handoffs to erase rejected intermediate evidence. Do not migrate remotely merely to match a local version number without inspecting actual chain/state. Do not treat a source hash mismatch on a historical proof as evidence the old run never happened; it means the current implementation differs and needs a new scoped proof.
