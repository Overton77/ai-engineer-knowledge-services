# Original provider reconciliation admission

EV-092, coordinator review, 2026-09-06. This slice establishes signed accounting-decision admission. It does not settle a provider attempt or adjust a budget. The complete mission remains active.

VerificationProviderReconciliationSchema binds the original tenant, operation, step, provider attempt, budget, provider/model, dispatch UUID and fencing token, request digest/full handle, execution handle, billing evidence handle, original dispatched/uncertain state and reserved amount. The operator decision specifies an actual cost, including a possible overrun, with synthetic_fixture or supplier_statement basis. Its only action is settle_original_attempt and redispatchAuthorized is always false. The Ed25519 seal has provider_accounting_only purpose. Operator/ticket identity and a positive validity interval of at most 24 hours are mandatory.

ProviderReconciliationAdmission accepts only server-owned tenant/provider/key/operator/basis grants. It freshly authorizes and hydrates canonical receipt bytes, validates signature and metadata/ancestry, verifies the retained execution's native binding, rejects supplier basis for synthetic execution, and hydrates exact request and bounded billing evidence bytes. Evidence cannot postdate issuance. It checks time and cancellation before returning a deeply frozen permit. A private per-instance WeakSet prevents cloned or cross-issuer permits from reaching the later persistence boundary; assertPermit also rechecks expiry. This admission has no accounting mutation or provider-dispatch port.

Migration 20260906032700 adds the dedicated verification_provider_reconciliation artifact type. Database-contract 0.2.27 is packaged and installed in KS; canonical and installed source/migrations/config/seed match across 190 files. Generated types and type checks pass. No settlement guard was weakened and no remote migration was applied. The existing ordinary settlement path still requires its original live worker authority.

The final local proof uses the two actual uncertain attempts from EV-091. It registers explicit synthetic billing fixtures and signed operator decisions, then tests valid admission, issuer/clone protection, unknown keys/operators, expiry, mismatched operation, supplier basis over synthetic execution and corrupted billing bytes. All six grouped checks pass. Original provider rows are compared before and after and remain unchanged. The independent audit verifies eight actual Storage artifacts, both signatures, receipt ancestry and exact original dispatch/accounting bindings, plus three current source hashes. These are synthetic operator decisions, not supplier statements or completed settlements.

Evidence in parent internal/:

- verification-provider-reconciliation-admission-42412e46-b5ab-4b28-97d1-799a11b63a18.json — SHA256 79b863f7d56e79a216b0ecee17ca35897741ac3073f0df9847a6a0119fda9b84.
- verification-provider-reconciliation-admission-audit-20260906.json — SHA256 65e61e7f341847be931479757aff106196fedcd77ee60fdc71275d170ec97552.
- verification-contract-0227-audit-20260906.json — SHA256 35236a9042e5920d74518761b0149b87e7185389eb5f68206e4e433886149331.

Use local helper mode provider-reconciliation-admission. Audit mode provider-reconciliation-admission-audit uses VERIFICATION_EXTRACTION_WORKER_RECEIPT with the final proof filename. A rerun produces newly issued decisions; historical decisions expire by design. Never load remote KS .env for local proofs. Initial proof failure on the unregistered artifact type led to the canonical migration; its preliminary fixture artifacts changed no accounting.

Next add the append-only reconciliation ledger and atomic persistence method. Under operation-first locking, it must recheck the permit's issuer/expiry and every original durable identity, execution mode, terminal failure/cancellation authority, registered receipt type and native signature binding. It must atomically release the original reservation, apply evidenced actual cost, and record exactly one immutable decision. Exact retries must return the original settlement; conflicting decisions must fail. Preserve original response handles, dispatch identity and signed publication snapshots. No reconciliation may authorize retry or redispatch. Then prove concurrency, rollback, overrun, cancellation and original-publication stability before adding operator transport/runtime admission. Broader verification-module requirements remain unchanged.

Validation: all72 workspace tasks passed uncached in2m8.257s before a final malformed-timestamp fix. After that fix, contract build/typecheck and all47 contract tests, the signed local proof and native audit passed again. The final regression requires malformed timestamps to return schema errors.
