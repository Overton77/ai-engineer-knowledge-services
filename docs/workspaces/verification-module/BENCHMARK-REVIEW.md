# Coordinator benchmark review

Status: in progress. Initial frozen Pilot version is preserved but not approved for a full live matrix or quality claims. No human-gold labels are established.

## Initial findings sent to owner

- BR-01: The initial freezer's provider-input example includes literal/adversarial tags and mutation names. Provider execution must receive input-only assertions, necessary exact source fragments and source context; expectation labels and transform identities must remain evaluator-only.
- BR-02: The offline demo returns expected answers directly and uses adversarial transform presence to select its baseline verdict. This is circular oracle replay. Delivered verification reports must derive outcomes from actual core execution or recorded provider outputs; hardcoded locator validity and uncomputed normalized-field acceptance cannot count as evidence.
- BR-03: Persisted checkpoints are loaded without schema, digest or identity validation. Their keys omit experiment/configuration/network bindings. Reuse must reject tampering, wrong run/case/arm/repetition, and configuration changes.
- BR-04: Annotation queues mark arbitrary matching adjudications complete. Completion must validate two distinct blinded human annotations and an independent expert, including strict schema and dataset/case binding.
- BR-05: Benchmark digest sorting uses locale-dependent comparison. Use the existing RFC 8785 canonical implementation and reject unsupported values.
- BR-06: The initial source/entity clusters are page keys. Related company pages and derivative study claims must remain grouped; paired sign-flip must respect clusters, and multiplicity correction must cover the complete prespecified hypothesis family.
- BR-07: Publication entailment does not establish world correctness. The initial paper literal expectations must retain an unknown or not-established world-truth state.
- BR-08: D013 limits combined case content to 2,000 UTF-16 characters, not excerpt alone, and the complete serialized provider request to 10,000 UTF-8 bytes. Both bounds require runtime validation before dispatch; qualification must not be truncated to fit.
- BR-09: Initial freeze and approval timestamps were fixed future midnight values. Record actual sealing time and D013 approval provenance in a versioned successor, preserving the original frozen files.

The owner has been instructed to resolve these findings before handoff and to hold provider dispatch until the input-only execution path is verified. The initial 40-case manifest is `sha256:e3529d2d27e3f473d4f6eb9da633b404c14c48f20b9db8c1d7a428220d538d9e`; its case-specific grant is `sha256:9ba42415f84a0c18d82b664bf4f4982b182e48c5e79602705e3dbf524f786c67`.

## Follow-up review and limited smoke release

BR-10 records compound initial assertions and absent-evidence/contradiction confusion. The owner preserved v1 and v2 and created `catalog/verification-benchmarks/diagnostics-companies-pilot-v3`: 43 cases, including 40 bounded provider-granted cases and three provider-excluded diagnostics. Source clusters now represent the two companies and two publication study families; none of these labels is human gold. The v3 dataset seal is `sha256:b625b311493f8c366ef43bf4047bf61f38d510fe7f3c340f50bca42de737dfe6`, grant `sha256:8c1530d5f856796ce71c21a71d592716fd2972fa53d4843235d7208798236639`, catalog seal `sha256:b90e96a2dc341f7d7c71f18d04f99fe4e9566b79c3b294effccf7dac585fa6ed`.

Coordinator approved this input scope for one case through the three configured candidates after runtime fixes, not the full matrix. Provider-visible identifiers must be opaque and contain no mutation labels. Private authority provenance, actual case digest recomputation, full-wire content/configuration validation and stopping on a failed smoke call are required. The shared budget and no-retry condition remain in force.

Independent grant probe `internal/verification-benchmark-grant-review-5e1e23e0-d487-4fae-9b31-df297e0ae842.json`, SHA-256 `47a92170b6f90b63ba00180257a494f029a375a6ce93d45f4fd8f107a9ca6624`, passes seven checks: input-only fields, copied authority rejection, altered assertion/fragment rejection, ungranted case rejection, unrelated small wire rejection and over-limit wire rejection. It performed zero provider dispatches.

Independent boundary probe initially found that a resealed matrix could conceal a missing case behind an extra repetition. Failed receipt `internal/verification-benchmark-boundary-review-da3c9fc7-8c68-4667-8966-9e8fdbe0fe8c.json`, SHA-256 `9b82f2259e9c1806bfb75dd224e4e554121af5864ff9e59b3cd8b13cb35330f2`, is retained. Corrected receipt `internal/verification-benchmark-boundary-review-283afdc1-c67a-4b3d-8dc2-ba79cc88d5f3.json`, SHA-256 `7df430cb25683799e23e0ffd6541ea048e866e26fc24d1b1561d658433c2ed6e`, passes all seven hashing/adjudication/checkpoint/Cartesian-coverage checks. These are bounded regressions, not final WS-07 acceptance.

Remaining review includes the actual smoke responses and accounting, full paired experiment, canonical dataset/run persistence, real extraction field derivations rather than whole-assertion string equality, independent statistics review, metric dimensions, report coverage and recorded-provider-output offline replay. The deterministic offline control is auxiliary; D-011's measured baseline uses Luna with deterministic mechanics.
# Independent smoke and boundary follow-up — 2026-09-05

The ten-check independent grant/wire review passed, including a real adapter request built with fake fetch as the positive control, changed assertion/fragment rejection, opaque authority rejection, wire size cap, and exact system/schema binding. Receipt: `internal/verification-benchmark-grant-review-d97dbe0c-d897-492f-942b-5ca15c6fb3f6.json`, SHA-256 `02410ffb340d00f381b57cd47bce50afb16e050f4e0a6625a4eb5f83018ac40a`. No external requests were made by that review.

Independent read-only hydration of all three actual smoke calls passed before the local reset: `internal/verification-benchmark-live-review-d9bec8ed-7380-449e-804d-08842436c7c8.json`, SHA-256 `eea987f6d64c39d4e7ba0944007d522a1c3ad0ab9905b31050b25fbdcd301923`. Luna reported partial support, Haiku not-supported/neutral with a qualifier boolean inconsistent with its own rationale, and Interfaze no support. These differences are retained as observed model outputs. They are not adjudicated gold.

The subsequent local reset and recovered physical bytes are documented in `LOCAL-RESET-RECOVERY.md`. Full matrix dispatch remains paused pending recovery and concrete runner review.

The coordinator separately applied all 90 migrations on a new disposable project, then ran seven dataset-manifest checks there: legacy compatibility, valid binding, wrong type/digest/tenant rejection, pending artifact rejection, and immutable contract downgrade rejection. Fresh-chain receipt `internal/fresh-chain-audit-benchmark-90-0905.json` has SHA-256 `85620c37197c7fac964328fa121f56db3c4a689bcf7e07d55e74244c769196cf`; seven-check receipt `internal/verification-dataset-migration-review-d7128dae-d78b-496f-ba59-e79a692d663a.json` has SHA-256 `474e86dd820fd4bfae1758b097aff7cc1539419d8e7837d18c6f22caa2bed5d6`. The exact isolated container was stopped with its volume retained. These tests do not resolve the separate populated legacy shared-bucket blocker B-002.

Canonical/vendor 0.2.7 parity also passed for five TypeScript sources, 90 active migrations and 50 archived/deferred SQL files, with the consumer pinned to that archive. Receipt: `internal/verification-vendor-parity-review-6addb5e7-d3cf-429e-9cdd-091bd012acdc.json`, SHA-256 `012b3887dee0ba788ccf169cd5ec92776088b90f1d59dcec60df46b282580ae5`. This is source/SQL byte parity; the subsequent local README/reset-guard package-script changes are not claimed as part of that frozen archive.

An independent forged-adjudication probe found that `freezeVerificationBenchmarkDataset` accepted a two-field stub plus arbitrary adjudication digest and allowed a case to claim human-gold eligibility without human annotations. Failure receipt: `internal/verification-human-gold-freeze-review-d202e120-0f6b-4013-af82-4859da0961f6.json`, SHA-256 `59ec0cbb3a16c03f03461edaa2327d1c442f5c3c19096de52654859dd98c9cb7`. The active owner is correcting this boundary; all current Pilot cases remain explicitly ineligible for human-gold scoring. Structural digest validation alone does not authenticate a human reviewer or establish adjudication provenance.

Two recorded-arm policy failures were also reproduced independently: failed field mechanics plus full semantic support, and omitted material qualifiers plus full semantic support, both produced `pass_with_warnings`. Failure receipt: `internal/verification-recorded-policy-review-6655ee95-1717-4797-8b53-97e6ad61ac87.json`, SHA-256 `83e377a0ae448cde086ac1b72efb3cc22293e5ff119237bc2ca8a61e34d9620e`. These are synthetic pure-composer probes with no provider calls. The active owner must close both admission gates and preserve separate semantic diagnostics before full experiment acceptance.

Both admission probes passed after correction in the coordinator's unchanged rerun: `internal/verification-recorded-policy-review-2eb0c59f-db90-4299-9505-6f61ddeb8c4e.json`, SHA-256 `bc66a80463dd27bb17d37b3ffdfd391ee18399bceb57c528d48752d62d6dd35b`. Each now returns non-admitted review with an explicit reason code. Source authority remains conservatively unassessed rather than inferred as sufficient from publication class. Future human adjudication must not override a failed deterministic field check; it must resolve the underlying evidence/input failure in a new run.

Canonical persistence preparation also found that v3's logical case-input IDs had no corresponding CAS bytes, while canonical verification cases require an expectation artifact. The owner is preparing a preserved v4 successor with actual materialized input and agent-generated expectation artifacts, a machine-readable v3-to-v4 diff and updated grant bindings. No v3 handle or seal may be retroactively rewritten. The three original smoke observations remain a separate v3 protocol result; the new extraction experiment may dispatch only the other 39 granted cases after configuration review.

## V4 materialization and independent human-label controls — 2026-09-05

The coordinator accepted V4's 95 catalog files and 86 materialized case artifacts after independent hash, seal, ID derivation and V3-to-V4 content comparison. Receipt `internal/verification-benchmark-v4-review-b4b84a37-fb2b-4509-af6d-102ba09f0006.json`, SHA-256 `a81a2109edc9ae1cc50b3f0d6749a903c31d5d627909efb396cce3e986401c0e`. The 43 cases retain agent engineering expectations and zero human gold.

Local canonical persistence was independently accepted by hydrating 87 complete artifact handles/bytes and checking all 43 case rows, dataset version, manifest, lifecycle and producer bindings. Receipt `internal/verification-benchmark-v4-persistence-review-f89a1d05-5f82-473f-825c-32988174c391.json`, SHA-256 `2cca8d2f9963f81193f344faced641817bb6c17eaee030bb87ac5c7e1eeccce5`. Complete replay pack SHA-256 `bc6ca4f4d41c711c80de911ace6c977107d8d298c704407464dcf08ad1f1594b`. This does not authorize schema-blind provider dispatch or claim remote deployment.

The stronger independent human-label probe now passes unchanged-label and expert-corrected-label positive controls, rejects mutation of the admitted source claim and rejects changes to unreviewed authority/world/policy dimensions. Receipt `internal/verification-human-gold-admission-review-7a7d4f75-4df8-42fd-85c0-e80f9aad8641.json`, SHA-256 `3565450df01736ef080e41f77c834565dedae3a76ff3190396f86c70fa57e689`. These are synthetic structural fixtures, not authenticated human annotations. Only the coarse label can be promoted through this artifact schema; other dimensions remain engineering expectations. Authentication of reviewer identity remains a service/review integration requirement.

Extraction experiment review additionally found that returned mutable experiment plans were stored in the private authority record by reference, permitting post-admission changes to requested field descriptions. The owner must snapshot that record and demonstrate rejection or isolation before paid dispatch. Exact field mechanics must bind the unique supplied evidence quote rather than ambiguously selecting a repeated leaf across the whole fragment.

The complete extraction wire matrix passed independent fake-fetch review: all 117 case/provider profiles used their actual adapters and exact grant guard, and all 117 appended-content mutations were rejected. Maximum serialized wire size was 3,878 bytes against the 10,000-byte cap. Receipt `internal/verification-extraction-wire-matrix-review-ad2cb4e3-ad8f-407e-b4e0-42b4b84ab9ae.json`, SHA-256 `8652b5058d900afd3fc32e7e38d61974b11d537dc6e80bcbc6b2919d5defe1ba`. No network or database writes occurred. The sealed experiment/schema and request bounds are accepted; execution still requires the authority snapshot fix and candidate-specific extraction/composer integration.

The returned-plan mutation finding was reproduced with a valid admitted-plan positive control and copied-authority rejection. Failure receipt `internal/verification-extraction-authority-review-d77ff7ec-374a-40de-87d1-2b5843613638.json`, SHA-256 `1eeb53744065fdfe55a2fe22a6fa4fb74ff590b1cfada067d2b676cd4af4c02a`. Keep this failure as regression evidence.

The coordinator reran the authority probe unchanged after the private-plan snapshot fix: all three checks pass. Receipt `internal/verification-extraction-authority-review-3db8631e-35ce-4f76-b6b4-a8148317985f.json`, SHA-256 `aef2ddb6c45c9191ef3de9a94e68f2fad6c1c0250863aaf2f0e4636587c0d158`. Source review confirms the composer now accepts strict extraction outputs separately from legacy support-only outputs and uses candidate-specific field mechanics. Exact leaf selectors are derived inside the unique supplied evidence quote. The runner must retain those machine selectors and complete fragment lineage in its per-field export.

## Held runner review — 2026-09-05

`prove-verification-benchmark-extraction-live.ts` was first reviewed with dispatch disabled. The fixed matrix, successor budget, accounted sink and per-call registration/export design are appropriate. Before execution, the owner is addressing: loopback host checks (not port checks alone), actual frozen selector/envelope admission before dispatch, immutable case/final receipt restart handling, complete available byte export on failures including Interfaze precontext, stable sanitized error codes, and cross-binding cached summaries to registered observation/field artifacts and raw responses. A safely captured model/schema/field quality failure must be a terminal recorded call outcome counted in paired denominators; it must not cause an automatic quality retry or unnecessarily cancel unrelated planned calls. Unclassified infrastructure/custody/accounting failures stop advancement.

Independent extraction composer review passes 16 candidate-mechanics combinations across the four arms, including deliberately inconsistent legacy scalar mechanics. Receipt `internal/verification-extraction-composer-review-95d157fb-8b5c-4465-91c4-6063625cb15c.json`, SHA-256 `1bb43cf50b0f30f45855d095365ae445e9f3f07747ecb50a1159bfb0829aca12`. Full support remains a separate diagnostic and cannot admit failed mechanics.

Report source review identifies remaining coverage, not acceptance: the current company-report generator lists selected statements and shared limitations, while its JSON lists case IDs and its HTML displays escaped Markdown. Specification 15.9.2/15.9.4 additionally requires company/product structured fields, algorithm/population/biomarker mappings, publication applicability and claim-by-claim evidence appendices. The audit needs all ten 15.9.5 paths. Exact leaf copying with an offset selector does not demonstrate normalized-field derivation merely because the experiment's field plan declares `normalized_text`. Preserve actual observed behavior and explicitly list missing measurements until concrete report/ledger paths are implemented and reviewed.

The coordinator's no-dispatch runner preflight authenticates all 39 native source selectors and reports exactly 117 calls with 2,925,000 microUSD maximum reservation. Receipt `internal/verification-extraction-runner-preflight-20260905-0841.json`. Its run ID is provisional because dispatchReviewDigest is still null; no DB or provider access occurred. A wrong copied V4 persistence-receipt digest was caught by direct file hashing and corrected before DB/live execution.

The coordinator added `verification-benchmark-response-replay.ts` to replay retained raw responses through the actual adapters using synthetic keys, in-memory fetch, exact wire guards and no persistence port. Its focused test covers all three valid profiles, wrong-model and HTTP500 failures, known/unknown Interfaze precontext, and verifies global fetch is never called. Test and application typecheck pass. The active owner must independently review this coordinator-authored helper before using it for restart/failed-checkpoint authentication. Source SHA-256 `ab9fdff3724e69d95ccff62babf248c200429c2d9b406ccc5c463533e6d25132`; test SHA-256 `b1c73a19859e274544c5b7a96b9dcd3f48d5c8e7249825041c2a1ca80d8f858a`.

## Bounded dispatch source review accepted — 2026-09-05

The coordinator accepted the concrete 39-case/117-call runner after the source, native selector, wire, per-field, budget, recovery, failure and restart reviews. Executable review receipt: `internal/verification-extraction-dispatch-review-a3a37105-72ac-4f46-bd64-b506ef62d6cf.json`, SHA-256 `8f0be395c61bbf877fbf0b89bf0ac9d044c3b25abf638654e723c9132d02b5b9`. Reviewed source snapshot uses the same UUID. The only gate-binding edit changed the null review digest initializer; final executable SHA-256 `92f392382c1c2c98400207edd0ca4f65a3a48630e93eb8e44978b167a7a565be`, normalized to reviewed source SHA-256 `45ce54269e27e4bd66bf8d2116e84fbc36cc12f49eb614992601d077178e0064`.

Earlier receipt `4e9de108-f17a-4868-b59a-448a2e7e68ae` / SHA-256 `6f8212304be2fd585916f0a0a509c7108093111bf99b35c47800a9e670ccae73` is preserved as superseded and unexecuted. A pre-binding hash check detected the owner's additional exact dataset-row validation edit and prevented stale approval from being used.

The WS-07 owner independently reviewed the coordinator's memory-only adapter replay helper and identified missing canonical precontext exposure. The helper now returns cloned admitted precontext bytes; runner replay binds them to registered precontext and envelope artifacts. Targeted tests and typechecks pass. Dispatch authorization is for the bounded lab matrix only; it does not accept unobserved outcomes or complete WS-07, human-gold, reports, remote deployment or service integration.

## Pre-dispatch full-handle mismatch — 2026-09-05

The first approved runner invocation stopped before keys/mission/provider dispatch because a V4 registry entry contains a partial artifact reference, while the runner's hydration helper requires a full immutable registered handle. Failure receipt `internal/verification-benchmark-extraction-pre-dispatch-failure-81599da5-97c7-46ff-9140-bd1173353944.json`, SHA-256 `426172310eeaa06dd6b2ff40f5a6238bf76ad6a92b0bf7acc3f5edc663a9748c`. The attempted output directory `internal/verification-benchmark-extraction-live-c17510df-a472-5099-a95a-c3aed9be31dc` is preserved.

The coordinator independently confirmed zero current provider attempts, zero extraction-run missions, and successor-budget reserved/settled balances of zero. Receipt `internal/verification-extraction-spend-snapshot-cd63ecf8-d6a3-408e-94b7-c76dbf3cd305.json`, SHA-256 `1cb6014c733ac3b8f97a37fa60e59234bbaf7a314af26c714ed95746487d9006`. Historical liabilities remain carried separately.

The correction must resolve full handles from the accepted, receipt-pinned V4 replay pack and verify every partial registry field against them. It must preserve strict full-handle DB/checkpoint equality, rather than weakening hydration to partial comparisons. A read-only local admission preflight should exercise canonical dataset, artifact and budget checks before mission creation or credentials. Changed runner code requires a successor source-review receipt; prior review must not authorize modified code.


## Coordinator complete live-run audit — 2026-09-05 09:13 UTC

Successor dispatch review `internal/verification-extraction-dispatch-review-8022bdd6-51e7-4e99-9c60-471e398db167.json` (SHA-256 `51bffa632ac44da2f7a6861b0b06b8f9e5b04ff5ab7fac29c9b7599605b4212e`) superseded the prior pre-dispatch failure. The coordinator independently passed all 39 native selectors, 43 dataset rows and 39 complete handles before binding only the reviewed null gate literal. Final runner SHA-256: `f679303fc7c7bc375739657b9b9e0df40b9a71f9dbd1d61aea6ebc0f3205a6df`.

The run `feeb824c-e4d0-597f-abd7-7667fa080869` completed 39 cases and 117 calls, with 110 accepted outputs and seven typed failures. No quality retry or terminal infrastructure failure occurred. Immutable replay pack digest: `sha256:9cff215cb598971a1b777cf7bc4d5f0e3ddce6e220bf2b37f50cdd616699bdb1`. The owner's exact second invocation returned the identical digest; independent complete custody audit found exactly 117 canonical attempts and a succeeded mission.

Coordinator actual-adapter offline audit: `internal/verification-extraction-independent-replay-588da3f2-4d4e-4a84-826e-09104e3fa84a.json`, SHA-256 `fbd6904fd5d55f413700945e22b6ba172deeff46dc29f760c0f447ac82b5fd0a`. All checkpoint/observation hashes, exported bytes, unique complete handle identities, request profiles, raw-envelope bindings, field-ledger recomputations and seven failure replays passed with zero network requests. Complete canonical/Storage audit: `internal/verification-extraction-custody-review-afa72941-8e8a-4bf8-8868-594bdc8d6e58.json`, SHA-256 `d464b181f13e763529fa43fa08707b9c4a2bd2415faa35e89bd66d1f35884064`. All 709 unique artifacts matched complete registered handles and physical Storage bytes. Exact current budget totals: 99,754 micro-USD settled and 1,950,000 micro-USD held. Failed calls are included; historical reset liability remains separate. Aggregate call latency is 490,923 ms, not wall-clock elapsed time.

These bounded execution/replay/accounting checks pass. Reports, statistical interpretation, full service integration and human-gold quality claims are not accepted by this entry. The coordinator-authored audit scripts are also submitted to the WS-07 owner for independent source review. Early audit-only assumptions about nonexistent handle.state and attempt.status fields were corrected before passing; no runner bytes or live evidence changed.


### Report v2 source/content review — not accepted

The coordinator rejected report pack `verification-benchmark-extraction-reports-v2-feeb824c-e4d0-597f-abd7-7667fa080869` (semantic manifest `sha256:13218b5e266cf05d87884bd3dd0af7c8aabd36d969956855f5eaa5db9f0aa22c`). Narrative publication and conflict sections selected adversarial cases by shared tags, presenting deliberately false trial/superiority/24-hour turnaround assertions as research statements. The appendix also omitted its JSON adversarial classification in Markdown. The ten named audit paths were not checked against actual outcomes: the selected turnaround mutation is a numerical contradiction, and the selected abstention is caused by a schema failure. A normalized-text comparison label does not by itself prove a value-changing normalization. Missing-observation composer outputs still classified schema failures as generic provider failures despite the separate correct requiredFailures ledger. Citation hashes were not navigable evidence links.

Owner is correcting these without new provider calls, preserving v2 and producing a new immutable report version. Complete provider-run execution/replay/custody acceptance above is unaffected; report acceptance remains pending.

## EV-035 — Coordinator WS-07 bounded acceptance

See `WS-07-INDEPENDENT-ACCEPTANCE.md` for independent replay/custody, V2/V3 rejection history, V5 machine and sampled browser acceptance, corrected V1 evidence binding, and exact receipt hashes. The 39-case/117-call engineering pilot is accepted with zero human gold. No whole-module, clinical, calibration, production, or cross-surface acceptance is implied. WS-08 implementation proceeds separately.
