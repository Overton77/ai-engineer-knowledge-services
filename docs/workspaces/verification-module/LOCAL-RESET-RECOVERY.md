# Local database reset and evidence recovery

On 2026-09-05 at approximately 07:15 UTC, the WS-07 agent ran `corepack pnpm db:reset` in `ai-engineer-db-contract` while applying the dataset-manifest migration. This cleared the populated local database. The coordinator had authorized the migration, not destructive replacement of accumulated evidence. Future migration proofs must use isolated instances, and populated development databases must receive incremental migrations after preservation checks.

The remote Supabase database was not changed. The local Storage volume survived. The agent restored 82 frozen-source artifacts and 16 captures from the prior immutable offline export before the coordinator's stop message. No post-reset provider calls or budget restoration occurred. All further database and Storage writes were paused for recovery review.

The incident receipt is `internal/verification-local-reset-incident-22ed52a4-680f-4e6a-a70c-9f164278b9ac.json` (SHA-256 `545c93fb4af9ff184e8a31830316bc9d8cb6d9ae6dd81954d128ef0874c2b7c2`). The source restoration receipt is `internal/verification-offline-source-restore-2d798327-8458-402c-8cbd-0145f44787bd.json` (SHA-256 `c1bdcaa6bad33a1d842d4b2d3149b2170ddbd9520addf0c1689e9de47e0f4335`). These are recovery events, not proof that the original database remained intact.

The coordinator copied `/mnt` from `supabase_storage_aiengineer` to `internal/verification-reset-recovery-ad277aa3-6d08-4a04-9c6c-e82957511e30/storage-volume`. Its immutable `inventory.json` (SHA-256 `7613ef8cb3802294f7c3175fd16af711bdd9cc2cd981020063263a3ce288601f`) hashes all 597 physical files and independently finds exact matches for all three WS-07 smoke raw responses and response envelopes. The pre-reset read-only review remains `internal/verification-benchmark-live-review-d9bec8ed-7380-449e-804d-08842436c7c8.json` (SHA-256 `eea987f6d64c39d4e7ba0944007d522a1c3ad0ab9905b31050b25fbdcd301923`). That receipt proves the original registrations, ordered parent bindings and seven provider-accounting entries as observed before reset; it does not contain every original database column.

Recovery must preserve the distinction between original wire bytes and newly imported custody metadata. Missing registration timestamps, signatures, producer bindings or accounting records must not be invented. Offline byte replay can cite the preserved pre-reset review and recovery inventory. Any new registration must explicitly identify the recovery incident and its new import provenance.

Before further paid dispatch, a reviewed persisted budget must retain the prior liabilities: 1,833 micro-USD settled and 2,070,000 micro-USD reserved/uncertain, totaling 2,071,833 micro-USD against the original 20,000,000 ceiling. A fresh budget may therefore have at most 17,928,167 micro-USD available while the previous liabilities remain separately documented. This is a conservative carry-forward limit, not reconstruction of the lost ledger. The Haiku smoke raw response contains billing evidence that may support separate reconciliation without inference; no reservation is released merely from a summary.

Status: recovery continuity accepted on 2026-09-05; the physical smoke bytes are wrapped in registered new-custody artifacts with a complete offline replay export. No acceptance claim relies on post-reset access to the lost original registrations.

Accepted import receipt: `internal/verification-reset-recovery-receipt-32254f8f-b81e-4dc3-aec6-ea1db03843a9.json`, SHA-256 `9c08f8c33131e844347bddd5b9711fffc1552d9127ea949e2746d25cf2cdf9b9`. The offline replay pack SHA-256 is `ea266c9d98d20303bccac3b99ce2991ad2f80f12121f13ee70bde69c347ef453`. Eight artifacts were staged and referenced by the accepted recovery activity. Its actual execution ran from 07:47:38.638 to 07:47:39.581 UTC. The successor budget is `5890d523-561b-55db-a724-890d642885d9`, key `ws07-pilot-recovery-20260905`, ceiling 17,928,167 micro-USD, with zero current reservations/settled charges at acceptance. The separate historical liabilities are not released.

Coordinator read-only review `internal/verification-recovery-independent-review-225e9af4-6a76-4376-8749-8db9077b210e.json`, SHA-256 `438ae9b9baade4f0d126755e329ce59727425e10e329f27eda70b6016eb49799`, independently hydrated all eight authorized registered artifacts, compared their handles and original wire/output bytes with the pre-reset review, checked recovery producer/mission bindings and actual lifecycle timestamps, confirmed all 82 source IDs remain available, and found exactly one successor budget and zero reconstructed historical provider attempts. No new inference was made. Idempotent owner re-entry returned the identical import receipt. B-006 is resolved for continued work; original metadata loss remains an explicit limitation.

The canonical README previously recommended `db:reset` in the ordinary local workflow. The coordinator corrected it to incremental `db:migrate` and added `scripts/reset-disposable-local.mjs`, which refuses the shared project and any target/override arguments before invoking the CLI. Three focused guard tests passed; the actual shared-project entry point refused without a database command. Receipt: `internal/verification-reset-guard-550c5338-0e00-407a-a2db-06b2abbe6dde.json`, SHA-256 `aacb3ae9d4a98ea5d6d6f86bdeaf51b8e4c5aceff148ee0eaa389203e4bdb10a`. This development guard does not claim to prevent direct CLI use by an administrator; preservation remains a required operating procedure.

The coordinator also recovered all four WS-06 synthetic conformance request/raw/envelope byte chains by exact digest from the preserved volume. The original pre-reset review hash matches EV-032. Recovery receipt: `internal/verification-ws06-recovery-review-0c29bb9e-deda-4302-9ba1-4e39ca0c1e00.json`, SHA-256 `c3c8ecef46a61c241279d7e8c7e75c03ae56dfeb3f31c43cabffd581f512138c`. This read-only recovery made no provider calls, reconstructed no registrations and changed no accounting. Together with the three WS-07 smoke calls, all seven original provider wire chains remain available as physical bytes under recorded recovery custody.

The old live proof entry points and the local wrapper's `provider-live` and `benchmark-live-smoke` modes now refuse execution when this immutable incident exists. The historical 20,000,000-microUSD pointer remains unchanged as evidence; it must not be recreated as a new spendable budget. The reviewed successor workflow carries prior liabilities explicitly.
