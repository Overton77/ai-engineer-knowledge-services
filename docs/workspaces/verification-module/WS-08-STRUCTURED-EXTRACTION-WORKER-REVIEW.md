# WS-08 configured structured extraction worker review

2026-09-06 UTC

EV-087 accepts the bounded configured structured-extraction worker composition. Public mutation transports, authenticated result reads, real process-kill recovery and broader module requirements remain unfinished.

`createConfiguredVerificationStructuredExtractionHandler` parses a bounded strict server-owned runtime config and registers only extract_and_register. It requires matching tenant grants, an explicit registered provider, parser/runtime identity, a matching signing private/public key and supported transport configuration. Synthetic execution requires an explicit in-process fetch port; ordinary startup cannot silently send live requests under synthetic custody. Missing configuration returns no handler. Live execution requires the selected provider credential. Duplicate request grants, tenant mismatch, missing credentials and signing/transport mismatch fail closed. The normal worker startup now adds this handler when configured; default configuration and public API admission stay closed.

The handler first authenticates any committed original publication through EV-086 and requires the exact configured runtime digest and execution mode before returning its original terminal result. For incomplete work it admits the registered native profile, preserves or binds original execution before any provider reservation, initializes the canonical lifecycle, and looks up ordinal0 through new lease-scoped `readOriginalAttempt`. An existing dispatched/uncertain/settled call is never dispatched again. Missing capture after dispatch requires reconciliation. New/reserved work uses the actual Gateway/Interfaze adapter, accounted sink and captured transport, then actual retained-response replay. Accepted replay retains candidate/provenance and success publication; failed replay retains a no-candidate failure checkpoint/publication. Known admitted usage settles cost, otherwise liability stays uncertain. The handler checks cancellation while working and stops its polling on exit. Recovery compatibility is exact runtime identity in this initial configuration, not an unreviewed cross-build migration allowlist.

`CanonicalDurableKnowledgeWorker` now recognizes the strict failed extraction result for extract_and_register and invokes failStructuredExtractionStep. Other outputs use normal completeStep. A captured failure is a durable result rather than a generic retry exception; SQL still independently requires the exact committed failure publication and terminal chain. The provider-accounting lookup requires an active operation-scoped lease and returns the original immutable logical-call identity; it has no dispatch authorization side effect.

Final real local proof97976363 passes12 named checks across accepted, HTTP503 and HTTP200/schema-invalid cases for both adapters. Each configured factory/registry/worker path issues one injected synthetic provider call. The test stops just after publication by overriding the terminal method and suppressing catch cleanup, verifies the operation remains running without a receipt, naturally expires its real lease, then resumes with the real canonical repository and a replacement worker. All six recoveries return the original publication and perform no new dispatch. Two operations/steps finish succeeded with unverified candidates; four finish failed with null candidates and automaticRetry false. Each has one exact receipt, and no terminal operation can be claimed again. This is in-process boundary fault injection, not an OS process-kill or live supplier test. The two accepted calls settle10 synthetic micros each; four failure calls retain400 total micros unknown-cost liability. Initial six-case proof700063ed is preserved as historical evidence; the final proof includes recovery. Reported totals cover the final six calls only.

The proof creates a unique registered schema description/profile while reusing the actual native source/projection chain, so provider input/wire bytes do not collide with earlier proof-only artifact policy metadata. It does not weaken the content-addressed artifact collision checks. An independent audit downloads44 actual Storage objects and checks full registered handles, canonical bytes, Ed25519 including tamper rejection, native execution/publication/failure/provider signatures, parents, seven scoped source files, exact SQL result reconstruction and six receipt/event/outbox/lease/operation/provider chains. Original dispatch fences remain older than the final replacement receipt fences. Tenant isolation passes and raw app_reader receipt-schema access remains denied. No broad repository/build provenance is claimed from the seven listed files.

Four focused runtime configuration tests, worker and proof typechecks, and all72 uncached workspace typecheck/test/build tasks pass (2m6.32s). No new migration or database package change was required; local326/database-contract0.2.26 remains current. Opt-in skipped tests are not runtime evidence. Startup registration is integrated and checked by the suite; this proof explicitly exercises the configured factory, registry and canonical worker in-process, not a production bootstrap process or remote deployment.

Next add extractStructuredData service/API admission from server-owned grants and readiness, mutation routes/typed client/CLI/MCP, and authenticated completed result reads for both terminal outcomes. Read APIs must authenticate immutable receipts and original publication bytes, return sanitized bounded results, enforce tenant/owner scope and keep raw receipt-schema access closed. Then perform actual process-kill/crash-gap recovery, cancellation/reconciliation, public-auth/security/deployment acceptance, arrays/dynamic evidence and all remaining semantic/authority/adjudication/case-score/export/human-review/runtime-cutover/dashboard requirements. The full46-row audit remains open; no whole acceptance row is closed by this worker proof.

Evidence in parent `internal/`:

- `verification-structured-extraction-worker-97976363-5591-40f7-b4a8-9d00fa805123.json`: SHA256 `6879adad44c8b3782e5ff986a60d48d5a29b07e3f66c3bd892c0f274f41dce4f`
- `verification-structured-extraction-worker-audit-20260906.json`: SHA256 `7b607d97ae0064b89bae1a59dc43d8f807dfb31401d5ec68b895bec6f66fb708`
- `verification-structured-extraction-worker-workspace-20260906.log`: SHA256 `8d778913c9c62ab0131da0e134a6b8f13fd3dd99d349265ae93432003a45567d`
