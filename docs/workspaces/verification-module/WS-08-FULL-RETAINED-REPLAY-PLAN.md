# Full retained-call replay — implementation handoff

Coordinator assessment, 2026-09-06 UTC. This plan follows accepted comparison service EV-074 and does not itself admit new execution or accept full replay.

The local pilot directory `../../../../internal/verification-benchmark-extraction-live-feeb824c-e4d0-597f-abd7-7667fa080869` contains exactly 117 distinct case/role checkpoints over 39 cases: 110 successful and seven captured schema failures. All seven failures have HTTP 200 and `PROVIDER_OUTPUT_SCHEMA_INVALID_CAPTURED`; six are Interfaze extractor calls and one is Haiku judgment. Their JSON checkpoints total 3,470,296 bytes; referenced artifact bytes total 1,358,458 before deduplication. These fit the existing replay bounds. Read-only inventory is `../../../../internal/verification-full-retained-replay-inventory-20260906.json`; registered database/Storage custody must still be checked during the proof.

Current `RegisteredBenchmarkReplayAdmission` only parses successful checkpoints. Its configured grant maps exact tenant/dataset/experiment references to exact checkpoint references and run identity hashes. Current public benchmark proof grants three successful retained calls; it does not replay the full pilot. `RegisteredDiagnosticsOfflineBenchmark` turns successful observations into recorded arm input and represents absent observations as unavailable. Simply omitting the seven failures would lose their replay/accounting attribution.

## Required change

1. Extend trusted registered replay admission with an explicit failed-checkpoint branch using the existing `DiagnosticsBenchmarkLiveFailedCallCheckpointSchema`. Preserve exact grant, checkpoint hash, run identity, case/dataset/experiment, source-input, full registered handle and actual byte checks. Enforce unique case/role across both successful and failed checkpoints.
2. Authenticate request/raw/envelope custody with the same canonical request digest, exact envelope keys, ordered parents and transformation signature as successful replay. Validate any paired precontext bytes/envelope. Failed checkpoints have no observation artifact; never fabricate one to satisfy the successful observation loader.
3. Reuse `assertDiagnosticsExtractionWireRequest` and `replayDiagnosticsCapturedResponse` with the trusted captured HTTP status. Require the exact captured adapter failure code; HTTP 200 does not imply successful schema validation. For the supported field-matrix failure variant, require adapter acceptance followed by the existing verifier's exact matrix rejection. No dispatch or automatic quality retry.
4. Return a discriminated result separating admitted observation from reproduced failure. Include immutable checkpoint/artifact references, captured failure attribution, historical accounting classification, and zero external requests. Extend executor provenance to preserve both counts and failures; only admitted observations enter `composeDiagnosticsRecordedArm`. Ensure existing three-call success behavior remains stable.
5. Register the complete trusted checkpoint set and a new offline experiment with exact successful observation references. Reuse the existing frozen dataset/profile/source custody and preserve old CAS artifacts/configurations. Do not mutate the v3 comparison inputs or reinterpret an allow-listed live experiment as an offline one.
6. Run an actual configured canonical benchmark worker over the full retained set, seal signed publication and verify all checkpoints/arm outputs and failure provenance. Keep human gold zero, recorded historical costs distinct from new charges, and provider request count zero. Audit failed-call admission negatives, current Storage bytes, canonical receipt and replay determinism. Compare to the retained pilot using its actual scope, not an assumed equality of fresh/live and later offline timing/accounting.

Existing filesystem-only failure replay logic is in `scripts/prove-verification-benchmark-extraction-live.ts` near `loadFailedCheckpoint`. Reuse its adapter/verifier primitives rather than importing that live-dispatch script. The registered production boundary must additionally retain strict artifact admission/parent/semantic binding. Actual process-kill comparison proof remains independently owned by verification_compare_kernel while coordinator owns this next-slice assessment.
