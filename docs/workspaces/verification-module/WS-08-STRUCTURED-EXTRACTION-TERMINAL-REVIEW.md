# Canonical structured extraction success receipts (EV-083)

Accepted bounded canonical structured-extraction success on 2026-09-06 UTC. The full verification-module goal remains active.

`VerificationStructuredExtractionResultSchema` and `createStructuredExtractionOperationResult` derive a strict result from an authenticated publication. The helper snapshots inputs before awaiting signature verification and checks native Ed25519, canonical body/full-manifest hashes, full publication handle, original time, ordered parents and transformation signature. The result carries the operation request digest, exact candidate/provenance/optional-precontext/execution references, manifest/provider-call digests and the full publication handle. Output remains `unverified_candidate` with `shape_only` validation. There is no independent unbound outputDigest field or quality claim.

`completeStep` now snapshots caller state and locks the structured-extraction operation before its lease and step. Under the live holder/token/fence and original step hash it supplies an exact transaction-local terminal claim. SQL reconstructs the entire expected result, including registered publication metadata, and hashes its canonical bytes. Its serializer is limited to this fixed-key, integer-only receipt shape; it is not a general JSON canonicalizer. A different output, extra field, substituted handle, unknown claim or stale lease cannot complete the step.

The step transition, lease release, success event, immutable receipt, outbox event and operation reconciliation remain one transaction. The receipt must match its original input hash, exact result hash, event, fence, executor and publication. A deferred constraint rejects a transaction that marks the step succeeded without its exact receipt and succeeded operation. Exact idempotent receipt reuse returns the original result. Initial/direct SQL success, orphan completion, duplicate receipts and post-completion mutations fail closed.

After execution binding, SQL also freezes operation request/hash, attempt/mission/work-item, routing/ownership/capability identity, idempotency/correlation/causation/actor identity and creation time. The original step input/hash/key/kind/identity, creation time and retry limit are frozen. Additional steps cannot be inserted or moved into the bound operation, and successful completion requires exactly one step. Normal status, lease heartbeat and cancellation fields remain mutable under their existing rules. Root review added322/323 for input and membership changes; independent review found omitted ownership fields, closed by follow-up324. Existing applied migrations were preserved. Migration321 replaces315's temporary blanket denial function with the reviewed publication/receipt checks while retaining its installed operation/step triggers.

Final actual local proof passes33 named checks. Both Gateway and Interfaze execute with injected synthetic responses, retain candidates/publications, recover under naturally expired higher-fence leases and complete through the real canonical `completeStep` method. Both operations and their sole steps are succeeded, each with one exact receipt. The proof includes observed pre-reservation row-lock races, actual accounting-state drift, typed source rejection, native SQL/body/hash equality, forged output variants, wrong holder/input digest, direct/deferred SQL denial, immutable owner/input/retry fields, and attempted step transfer. A third synthetic call establishes reservation-wins/late-binding denial and is cancelled afterward. All three calls settle synthetic10-micro costs at ordinal0. Zero supplier requests, live billing, process-kill recovery or full historical-budget accounting are claimed.

The independent audit downloads27 actual Storage objects, compares complete registered handles, authenticates native signatures/parents/cost evidence and nineteen retained source files, and verifies each exact receipt/event/outbox/lease/operation chain. It independently hashes canonical operation requests and step inputs and compares thirteen actual SQL function bodies with migration source. Publication/lifecycle app_reader tenant isolation passes; direct receipt-schema access is denied for app_reader and remains denied. Authenticated service reads are a subsequent implementation, not a new database grant.

Final contract and application suites contain40 and188 passing tests respectively. Standalone proof typecheck, generated database types check, database typecheck, and all72 uncached workspace typecheck/test/build tasks pass (2m6.127s). Local database-contract0.2.24 matches187 shipped canonical files including321-324. Intermediate proof0dedab/520eb and package0.2.23 are superseded by the final evidence. No remote migration was run; skipped opt-in tests are not runtime evidence.

Evidence in parent `internal/`:

- `verification-structured-extraction-terminal-6f4b2e50-9b1d-4d6a-9ed6-136ea7a99b1a.json`: SHA256 `e8a57b1d27fb5b012969dae2484bf1c8b8c0f3b26e5e00c45de934031ef161cb`
- `verification-structured-extraction-terminal-audit-20260906.json`: SHA256 `be94f63dbefaeea568375361dd6f4a11962e7a68e48abf8295990079fec14851`
- `verification-structured-extraction-terminal-workspace-20260906.log`: SHA256 `524b2169c11ed306030f7f53fff9e79559a17fa7c74069b60e2edc5b3cab905b`
- `verification-contract-0224-audit-20260906.json`: SHA256 `015b4744fba873185622f1a3cd0cca6c5211c8ba8fb4408a4102e58bcd4ea710`

Next: durable no-candidate outcomes for captured provider HTTP/schema failures, their signed custody and exact failed receipts. Then configure the actual extractor executor/worker and HTTP/client/CLI/MCP/authenticated reads, followed by actual process-kill windows and supplier reconciliation. Public/default extraction admission remains closed until that runtime is configured and proven. A replacement worker must preserve the original execution identity and validate its compatibility with the actual recovery implementation. Array evidence mapping, claims/report/adjudication, canonical case/score completeness, full disconnected replay, security/deployment/runtime cutovers, dashboard/human review/calibration and the46-row completion audit remain open. No entire acceptance row is closed by this bounded terminal proof.
