---
id: dom:identity
kind: domain
schemas: [corpus]
aliases: [entities, identity, canonical entity, resolve]
---

# Identity

`corpus.entity` is the identity; `kind` picks the typed table. Resolve names with `q:entity.resolve` before creating anything.
