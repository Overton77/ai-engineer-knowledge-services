{
  "name": "@aiengineer/knowledge-db-read",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Catalog-driven read executor: knowledge-read-intent.v1 → knowledge-read-snapshot.v1 under bounded roles, plus guarded read-only SQL, EXPLAIN, head, and artifact ledger writes.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm && tsc --emitDeclarationOnly --rootDir src --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aiengineer/knowledge-persistence": "workspace:*",
    "@aiengineer/knowledge-runtime": "workspace:*",
    "@aiengineer/knowledge-schema-workspace": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
