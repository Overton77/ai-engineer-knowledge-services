# Derived engineering mutations v1

The offline diagnostics command emits `engineering-mutations.json` from four bounded recipes in `verification-benchmark.ts`: a company name, CpG biomarker wording, a named institution, and a selected citation digest. Original literals must occur in the exact admitted selection. The native exact-field verifier evaluates original and changed candidates; citation tampering changes the selected digest while preserving the candidate text.

Every output record binds the source, projection, transformation, selector and selected digest and retains native check results. The command output manifest seals these generated records. This directory is descriptive documentation, not a replacement frozen benchmark dataset or human annotation pack. Original v1 cases and labels remain unchanged. Mechanical degradation does not establish semantic contradiction or full VR-041 acceptance.

The root coordinator replaced the initial incomplete runner and validated five focused regressions: actual replacement, absent/unchanged literal, off-selector text, citation digest tamper, and forged admission binding. See the root offline completion ledger for current integration evidence.
